[package]
name = "aerospike_async"
version = "0.6.0-alpha.6"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[lib]
name = "_aerospike_async_native"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.29", default-features = false }
pyo3-async-runtimes = { version = "0.29", features = ["tokio-runtime"] }
tokio = "1.40"
aerospike-core = { git = "https://github.com/aerospike/aerospike-client-rust", branch = "v3", package = "aerospike-core", features = ["rt-tokio", "dynamic-config"] }
hex = "0.4"
ordered-float = { version = "3", default-features = false }
pyo3-stub-gen = "0.23"
log = "0.4"
pyo3-log = "0.13"
futures = "0.3"
tokio-rustls = { version = "0.26", optional = true }
rustls = { version = "0.23", optional = true }
rustls-pemfile = { version = "2", optional = true }
webpki-roots = { version = "1.0", optional = true }
aws-lc-sys = "0.37.0"
aws-lc-rs = "1.15.4"
mimalloc = "0.1"
rand = { version = "0.8", features = ["small_rng"] }

[features]
default = ["extension-module", "tls"]
extension-module = ["pyo3/extension-module"]
tls = ["aerospike-core/tls", "tokio-rustls", "rustls", "rustls-pemfile", "webpki-roots"]

[[bin]]
name = "stub_gen"
path = "src/bin/stub_gen.rs"

[profile.release]
strip = "debuginfo"
lto = "thin"
codegen-units = 1

# Local development override — point at the working-tree rust-core to pick
# up the uncommitted fix being staged there. Strip this block before
# committing to dev.
[patch."https://github.com/aerospike/aerospike-client-rust"]
aerospike-core = { path = "/Users/pbrackin/tmp/aerospike-client-rust/aerospike-core" }
aerospike-rt = { path = "/Users/pbrackin/tmp/aerospike-client-rust/aerospike-rt" }
