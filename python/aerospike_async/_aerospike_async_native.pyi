# This file is automatically generated by pyo3_stub_gen
# ruff: noqa: E501, F401, F403, F405

import _aerospike_async_native
import builtins
import enum
import typing
__all__ = [
    "AbortStatus",
    "AdminPolicy",
    "AuthMode",
    "BasePolicy",
    "BatchDeleteOp",
    "BatchDeletePolicy",
    "BatchPolicy",
    "BatchReadOp",
    "BatchReadPolicy",
    "BatchRecord",
    "BatchRecordStream",
    "BatchUDFPolicy",
    "BatchWriteOp",
    "BatchWritePolicy",
    "BitOperation",
    "BitPolicy",
    "BitWriteFlags",
    "BitwiseOverflowActions",
    "BitwiseResizeFlags",
    "Blob",
    "CTX",
    "CdtOperation",
    "Client",
    "ClientPolicy",
    "CollectionIndexType",
    "CommitLevel",
    "CommitStatus",
    "DropIndexTask",
    "ExecuteTask",
    "ExpOperation",
    "ExpReadFlags",
    "ExpType",
    "ExpWriteFlags",
    "Expiration",
    "FastRng",
    "Filter",
    "FilterExpression",
    "GenerationPolicy",
    "GeoJSON",
    "HLL",
    "HLLPolicy",
    "HLLWriteFlags",
    "HllOperation",
    "IndexTask",
    "IndexType",
    "Key",
    "List",
    "ListOperation",
    "ListOrderType",
    "ListPolicy",
    "ListReturnType",
    "ListSortFlags",
    "ListWriteFlags",
    "LoopVarPart",
    "Map",
    "MapOperation",
    "MapOrder",
    "MapPolicy",
    "MapReturnType",
    "MapWriteFlags",
    "MapWriteMode",
    "ModifyFlags",
    "Node",
    "Operation",
    "PartitionFilter",
    "PartitionStatus",
    "Privilege",
    "PrivilegeCode",
    "QueryDuration",
    "QueryPlan",
    "QueryPolicy",
    "QuerySelection",
    "QueryWhereFlags",
    "ReadModeAP",
    "ReadModeSC",
    "ReadPolicy",
    "Record",
    "RecordExistsAction",
    "Recordset",
    "RegexFlag",
    "RegisterTask",
    "Replica",
    "ResultCode",
    "Role",
    "SelectFlags",
    "ServerError",
    "SpecialValue",
    "Statement",
    "TaskStatus",
    "TlsConfig",
    "Txn",
    "TxnState",
    "UDFLang",
    "UdfRemoveTask",
    "User",
    "Version",
    "WritePolicy",
    "_LocalClient",
    "geojson",
    "has_any_write_op",
    "new_client",
    "new_client_blocking",
    "null",
]
class AdminPolicy:
    @property
    def timeout(self) -> builtins.int: ...
    @timeout.setter
    def timeout(self, value: builtins.int) -> None: ...
    def __new__(cls) -> _aerospike_async_native.AdminPolicy: ...

class BasePolicy:
    @property
    def read_mode_ap(self) -> _aerospike_async_native.ReadModeAP: ...
    @read_mode_ap.setter
    def read_mode_ap(self, value: _aerospike_async_native.ReadModeAP) -> None: ...
    @property
    def read_mode_sc(self) -> _aerospike_async_native.ReadModeSC: ...
    @read_mode_sc.setter
    def read_mode_sc(self, value: _aerospike_async_native.ReadModeSC) -> None: ...
    @property
    def total_timeout(self) -> builtins.int: ...
    @total_timeout.setter
    def total_timeout(self, value: builtins.int) -> None: ...
    @property
    def max_retries(self) -> builtins.int: ...
    @max_retries.setter
    def max_retries(self, value: builtins.int) -> None: ...
    @property
    def sleep_between_retries(self) -> builtins.int: ...
    @sleep_between_retries.setter
    def sleep_between_retries(self, value: builtins.int) -> None: ...
    @property
    def filter_expression(self) -> typing.Optional[_aerospike_async_native.FilterExpression]: ...
    @filter_expression.setter
    def filter_expression(self, value: typing.Optional[_aerospike_async_native.FilterExpression]) -> None: ...
    @property
    def socket_timeout(self) -> builtins.int: ...
    @socket_timeout.setter
    def socket_timeout(self, value: builtins.int) -> None: ...
    @property
    def use_compression(self) -> builtins.bool: ...
    @use_compression.setter
    def use_compression(self, value: builtins.bool) -> None: ...
    @property
    def compression_threshold(self) -> builtins.int:
        r"""
        Minimum command-buffer size (bytes) at which compression actually
        fires. Buffers `<=` this value are sent uncompressed even when
        ``use_compression`` is ``True``. Default: ``128``.
        """
    @compression_threshold.setter
    def compression_threshold(self, value: builtins.int) -> None: ...
    @property
    def txn(self) -> typing.Optional[_aerospike_async_native.Txn]: ...
    @txn.setter
    def txn(self, value: typing.Optional[_aerospike_async_native.Txn]) -> None: ...
    @property
    def read_touch_ttl(self) -> builtins.int: ...
    @read_touch_ttl.setter
    def read_touch_ttl(self, value: builtins.int) -> None: ...
    def __new__(cls) -> _aerospike_async_native.BasePolicy: ...

@typing.final
class BatchDeleteOp:
    r"""
    A single delete operation for use with :meth:`Client.batch`.
    """
    def __new__(cls, key: _aerospike_async_native.Key, policy: typing.Optional[_aerospike_async_native.BatchDeletePolicy] = None) -> _aerospike_async_native.BatchDeleteOp: ...

class BatchDeletePolicy:
    @property
    def filter_expression(self) -> typing.Optional[_aerospike_async_native.FilterExpression]: ...
    @filter_expression.setter
    def filter_expression(self, value: typing.Optional[_aerospike_async_native.FilterExpression]) -> None: ...
    @property
    def send_key(self) -> builtins.bool: ...
    @send_key.setter
    def send_key(self, value: builtins.bool) -> None: ...
    @property
    def durable_delete(self) -> builtins.bool: ...
    @durable_delete.setter
    def durable_delete(self, value: builtins.bool) -> None: ...
    @property
    def generation(self) -> builtins.int: ...
    @generation.setter
    def generation(self, value: builtins.int) -> None: ...
    def __new__(cls) -> _aerospike_async_native.BatchDeletePolicy: ...

class BatchPolicy(_aerospike_async_native.BasePolicy):
    ...

@typing.final
class BatchReadOp:
    r"""
    A single read operation for use with :meth:`Client.batch`.
    """
    def __new__(cls, key: _aerospike_async_native.Key, bins: typing.Optional[typing.Sequence[builtins.str]] = None, operations: typing.Optional[typing.Sequence[typing.Any]] = None, policy: typing.Optional[_aerospike_async_native.BatchReadPolicy] = None) -> _aerospike_async_native.BatchReadOp: ...

class BatchReadPolicy:
    @property
    def filter_expression(self) -> typing.Optional[_aerospike_async_native.FilterExpression]: ...
    @filter_expression.setter
    def filter_expression(self, value: typing.Optional[_aerospike_async_native.FilterExpression]) -> None: ...
    @property
    def read_touch_ttl(self) -> builtins.int: ...
    @read_touch_ttl.setter
    def read_touch_ttl(self, value: builtins.int) -> None: ...
    def __new__(cls) -> _aerospike_async_native.BatchReadPolicy: ...

class BatchRecord:
    @property
    def key(self) -> _aerospike_async_native.Key: ...
    @property
    def record(self) -> typing.Optional[_aerospike_async_native.Record]:
        r"""
        Get the record from this batch result.

        **Performance Note:** This method clones the Record data on each call.
        The amount of data cloned depends on the record size (bins, metadata, etc.).
        For optimal performance when accessing the record multiple times, cache the result in Python:

        results = await client.batch_read(bp, brp, keys, None)
        for batch_record in results:
            record = batch_record.record  # Clone once
            if record:
                # Use record multiple times - no additional cloning
                bins = record.bins
                key = record.key
                generation = record.generation

        Returns:
            Optional[Record]: The record if present, None otherwise.
        """
    @property
    def result_code(self) -> typing.Optional[_aerospike_async_native.ResultCode]: ...
    @property
    def in_doubt(self) -> builtins.bool: ...

class BatchRecordStream:
    r"""
    Async/sync iterator over a streaming batch result.

    Items arrive in **completion order** (the node that responds first
    yields first), not input order — each item is a `(idx, BatchRecord)`
    tuple carrying the position of the originating op in the input list.
    Per-key errors land on the `BatchRecord` (`result_code`); cluster-level
    errors raise from `__anext__` / `__next__`. The stream ends with
    `StopAsyncIteration` / `StopIteration` once every input op has yielded.

    **Threading**: the receiver is held behind a single Mutex and
    `__anext__` / `__next__` hold the lock across the await/poll. This
    is intentional — the design assumes a single consumer driving the
    stream end-to-end. Fan-out consumers would serialize at the lock.

    **Bridge routing**: an async-constructed stream
    (`Client.batch_stream`) carries the originating Client's
    `CompletionBridge`, so each `__anext__` future is loop-affinity-
    checked and spawned on the same per-Client Tokio runtime as the
    Client itself — preserving AsyncPool's per-Client runtime isolation
    invariant for streaming. A sync-constructed stream
    (`Client.batch_stream_blocking`) has no bridge and rejects
    `__anext__` with a clear error: use sync iteration on those, or
    recreate the stream via `batch_stream` in an async context.
    """
    def __aiter__(self) -> _aerospike_async_native.BatchRecordStream: ...
    def __anext__(self) -> typing.Awaitable[typing.Tuple[builtins.int, BatchRecord]]: ...
    def __iter__(self) -> _aerospike_async_native.BatchRecordStream: ...
    def __next__(self) -> tuple[builtins.int, _aerospike_async_native.BatchRecord]: ...
    def close(self) -> None:
        r"""
        Release the batch stream early.

        Latches the stream closed so any subsequent iteration terminates
        with ``StopAsyncIteration`` / ``StopIteration``, and eagerly drops
        the receiver together with any buffered-but-unconsumed results —
        deterministically, rather than waiting for garbage collection.

        **Scope**: this does *not* cancel per-node batch requests already
        in flight. Those complete in the background and release their
        connections as they finish; ``close()`` only reclaims the consumer
        side (receiver + buffer). Idempotent, and safe to call from either
        an async or a blocking context.

        If a yield is in progress at the instant of the call (an internal
        lock is held), the eager receiver-drop is skipped and cleanup falls
        back to normal drop semantics once that yield settles; the closed
        latch still takes effect immediately.
        """

class BatchUDFPolicy:
    @property
    def filter_expression(self) -> typing.Optional[_aerospike_async_native.FilterExpression]: ...
    @filter_expression.setter
    def filter_expression(self, value: typing.Optional[_aerospike_async_native.FilterExpression]) -> None: ...
    @property
    def send_key(self) -> builtins.bool: ...
    @send_key.setter
    def send_key(self, value: builtins.bool) -> None: ...
    @property
    def durable_delete(self) -> builtins.bool: ...
    @durable_delete.setter
    def durable_delete(self, value: builtins.bool) -> None: ...
    def __new__(cls) -> _aerospike_async_native.BatchUDFPolicy: ...

@typing.final
class BatchWriteOp:
    r"""
    A single write operation for use with :meth:`Client.batch`.
    """
    def __new__(cls, key: _aerospike_async_native.Key, operations: typing.Sequence[typing.Any], policy: typing.Optional[_aerospike_async_native.BatchWritePolicy] = None) -> _aerospike_async_native.BatchWriteOp: ...

class BatchWritePolicy:
    @property
    def filter_expression(self) -> typing.Optional[_aerospike_async_native.FilterExpression]: ...
    @filter_expression.setter
    def filter_expression(self, value: typing.Optional[_aerospike_async_native.FilterExpression]) -> None: ...
    @property
    def send_key(self) -> builtins.bool: ...
    @send_key.setter
    def send_key(self, value: builtins.bool) -> None: ...
    @property
    def durable_delete(self) -> builtins.bool: ...
    @durable_delete.setter
    def durable_delete(self, value: builtins.bool) -> None: ...
    @property
    def generation(self) -> builtins.int: ...
    @generation.setter
    def generation(self, value: builtins.int) -> None: ...
    @property
    def expiration(self) -> _aerospike_async_native.Expiration: ...
    @expiration.setter
    def expiration(self, value: _aerospike_async_native.Expiration) -> None: ...
    @property
    def record_exists_action(self) -> _aerospike_async_native.RecordExistsAction: ...
    @record_exists_action.setter
    def record_exists_action(self, value: _aerospike_async_native.RecordExistsAction) -> None: ...
    @property
    def generation_policy(self) -> _aerospike_async_native.GenerationPolicy: ...
    @generation_policy.setter
    def generation_policy(self, value: _aerospike_async_native.GenerationPolicy) -> None: ...
    def __new__(cls) -> _aerospike_async_native.BatchWritePolicy: ...

class BitOperation:
    r"""
    Bit operations. Create bit operations used by the client's `operate()` method.
    """
    @staticmethod
    def resize(bin_name: builtins.str, byte_size: builtins.int, resize_flags: typing.Optional[_aerospike_async_native.BitwiseResizeFlags], policy: _aerospike_async_native.BitPolicy) -> _aerospike_async_native.BitOperation:
        r"""
        Create a Bit resize operation (resizes byte array, requires BitPolicy).
        """
    @staticmethod
    def insert(bin_name: builtins.str, byte_offset: builtins.int, value: typing.Any, policy: _aerospike_async_native.BitPolicy) -> _aerospike_async_native.BitOperation:
        r"""
        Create a Bit insert operation (inserts bytes, requires BitPolicy).
        """
    @staticmethod
    def remove(bin_name: builtins.str, byte_offset: builtins.int, byte_size: builtins.int, policy: _aerospike_async_native.BitPolicy) -> _aerospike_async_native.BitOperation:
        r"""
        Create a Bit remove operation (removes bytes, requires BitPolicy).
        """
    @staticmethod
    def set(bin_name: builtins.str, bit_offset: builtins.int, bit_size: builtins.int, value: typing.Any, policy: _aerospike_async_native.BitPolicy) -> _aerospike_async_native.BitOperation:
        r"""
        Create a Bit set operation (sets bits, requires BitPolicy).
        """
    @staticmethod
    def or_(bin_name: builtins.str, bit_offset: builtins.int, bit_size: builtins.int, value: typing.Any, policy: _aerospike_async_native.BitPolicy) -> _aerospike_async_native.BitOperation:
        r"""
        Create a Bit or operation (performs bitwise OR, requires BitPolicy).
        """
    @staticmethod
    def xor(bin_name: builtins.str, bit_offset: builtins.int, bit_size: builtins.int, value: typing.Any, policy: _aerospike_async_native.BitPolicy) -> _aerospike_async_native.BitOperation:
        r"""
        Create a Bit xor operation (performs bitwise XOR, requires BitPolicy).
        """
    @staticmethod
    def and_(bin_name: builtins.str, bit_offset: builtins.int, bit_size: builtins.int, value: typing.Any, policy: _aerospike_async_native.BitPolicy) -> _aerospike_async_native.BitOperation:
        r"""
        Create a Bit and operation (performs bitwise AND, requires BitPolicy).
        """
    @staticmethod
    def not_(bin_name: builtins.str, bit_offset: builtins.int, bit_size: builtins.int, policy: _aerospike_async_native.BitPolicy) -> _aerospike_async_native.BitOperation:
        r"""
        Create a Bit not operation (performs bitwise NOT, requires BitPolicy).
        """
    @staticmethod
    def lshift(bin_name: builtins.str, bit_offset: builtins.int, bit_size: builtins.int, shift: builtins.int, policy: _aerospike_async_native.BitPolicy) -> _aerospike_async_native.BitOperation:
        r"""
        Create a Bit lshift operation (performs left shift, requires BitPolicy).
        """
    @staticmethod
    def rshift(bin_name: builtins.str, bit_offset: builtins.int, bit_size: builtins.int, shift: builtins.int, policy: _aerospike_async_native.BitPolicy) -> _aerospike_async_native.BitOperation:
        r"""
        Create a Bit rshift operation (performs right shift, requires BitPolicy).
        """
    @staticmethod
    def add(bin_name: builtins.str, bit_offset: builtins.int, bit_size: builtins.int, value: builtins.int, signed: builtins.bool, action: _aerospike_async_native.BitwiseOverflowActions, policy: _aerospike_async_native.BitPolicy) -> _aerospike_async_native.BitOperation:
        r"""
        Create a Bit add operation (adds to integer value, requires BitPolicy).
        """
    @staticmethod
    def subtract(bin_name: builtins.str, bit_offset: builtins.int, bit_size: builtins.int, value: builtins.int, signed: builtins.bool, action: _aerospike_async_native.BitwiseOverflowActions, policy: _aerospike_async_native.BitPolicy) -> _aerospike_async_native.BitOperation:
        r"""
        Create a Bit subtract operation (subtracts from integer value, requires BitPolicy).
        """
    @staticmethod
    def set_int(bin_name: builtins.str, bit_offset: builtins.int, bit_size: builtins.int, value: builtins.int, policy: _aerospike_async_native.BitPolicy) -> _aerospike_async_native.BitOperation:
        r"""
        Create a Bit set_int operation (sets integer value, requires BitPolicy).
        """
    @staticmethod
    def get(bin_name: builtins.str, bit_offset: builtins.int, bit_size: builtins.int) -> _aerospike_async_native.BitOperation:
        r"""
        Create a Bit get operation (gets bits, read-only).
        """
    @staticmethod
    def count(bin_name: builtins.str, bit_offset: builtins.int, bit_size: builtins.int) -> _aerospike_async_native.BitOperation:
        r"""
        Create a Bit count operation (counts set bits, read-only).
        """
    @staticmethod
    def lscan(bin_name: builtins.str, bit_offset: builtins.int, bit_size: builtins.int, value: builtins.bool) -> _aerospike_async_native.BitOperation:
        r"""
        Create a Bit lscan operation (scans left for value, read-only).
        """
    @staticmethod
    def rscan(bin_name: builtins.str, bit_offset: builtins.int, bit_size: builtins.int, value: builtins.bool) -> _aerospike_async_native.BitOperation:
        r"""
        Create a Bit rscan operation (scans right for value, read-only).
        """
    @staticmethod
    def get_int(bin_name: builtins.str, bit_offset: builtins.int, bit_size: builtins.int, signed: builtins.bool) -> _aerospike_async_native.BitOperation:
        r"""
        Create a Bit get_int operation (gets integer value, read-only).
        """

class BitPolicy:
    def __new__(cls, write_flags: typing.Optional[typing.Any] = None) -> _aerospike_async_native.BitPolicy:
        r"""
        Create a new BitPolicy with the specified write flags.
        write_flags may be BitWriteFlags or int (bitmask), e.g. CREATE_ONLY | NO_FAIL.
        Default is default write flags.
        """
    def get_write_flags(self) -> builtins.int:
        r"""
        Get the write flags.
        """
    def set_write_flags(self, flags: typing.Any) -> None:
        r"""
        Set the write flags.
        flags may be BitWriteFlags or int (bitmask).
        """

class Blob:
    @property
    def value(self) -> builtins.list[builtins.int]: ...
    @value.setter
    def value(self, value: typing.Sequence[builtins.int]) -> None: ...
    def __new__(cls, v: typing.Sequence[builtins.int]) -> _aerospike_async_native.Blob: ...
    def as_string(self) -> builtins.str:
        r"""
        Returns a string representation of the value.
        """
    def __getitem__(self, idx: builtins.int) -> builtins.int: ...
    def __setitem__(self, idx: builtins.int, v: builtins.int) -> None: ...
    def __hash__(self) -> builtins.int: ...
    def __richcmp__(self, other: typing.Any, op: int) -> builtins.bool: ...
    def __add__(self, other: typing.Any) -> _aerospike_async_native.Blob: ...
    def __mul__(self, other: typing.Any) -> _aerospike_async_native.Blob: ...
    def __iadd__(self, other: typing.Any) -> None: ...
    def __imul__(self, other: typing.Any) -> None: ...
    def __delitem__(self, idx: builtins.int) -> None: ...
    def __len__(self) -> builtins.int: ...

@typing.final
class CTX:
    r"""
    Context for nested CDT (Complex Data Type) operations.
    Used to specify the location of nested lists/maps within a record.
    """
    def __eq__(self, other: _aerospike_async_native.CTX) -> builtins.bool:
        r"""
        Compare two CTX objects for equality.
        """
    @staticmethod
    def list_index(index: builtins.int) -> _aerospike_async_native.CTX:
        r"""
        Lookup list by index offset.
        If the index is negative, the resolved index starts backwards from end of list.
        Examples: 0 = first item, 4 = fifth item, -1 = last item, -3 = third to last item.
        """
    @staticmethod
    def list_index_create(index: builtins.int, order: _aerospike_async_native.ListOrderType, pad: builtins.bool) -> _aerospike_async_native.CTX:
        r"""
        Create list with given type at index offset, given an order and pad.
        """
    @staticmethod
    def list_rank(rank: builtins.int) -> _aerospike_async_native.CTX:
        r"""
        Lookup list by rank.
        0 = smallest value, N = Nth smallest value, -1 = largest value.
        """
    @staticmethod
    def list_value(value: typing.Any) -> _aerospike_async_native.CTX:
        r"""
        Lookup list by value.
        """
    @staticmethod
    def map_index(index: builtins.int) -> _aerospike_async_native.CTX:
        r"""
        Lookup map by index offset.
        If the index is negative, the resolved index starts backwards from end of list.
        """
    @staticmethod
    def map_rank(rank: builtins.int) -> _aerospike_async_native.CTX:
        r"""
        Lookup map by rank.
        0 = smallest value, N = Nth smallest value, -1 = largest value.
        """
    @staticmethod
    def map_key(key: typing.Any) -> _aerospike_async_native.CTX:
        r"""
        Lookup map by key.
        """
    @staticmethod
    def map_key_create(key: typing.Any, order: _aerospike_async_native.MapOrder) -> _aerospike_async_native.CTX:
        r"""
        Create map with given type at map key.
        """
    @staticmethod
    def map_value(value: typing.Any) -> _aerospike_async_native.CTX:
        r"""
        Lookup map by value.
        Converts HashMap to BTreeMap (OrderedMap) for exact byte-level matching to ensure consistent serialization.
        """
    @staticmethod
    def all_children() -> _aerospike_async_native.CTX:
        r"""
        Select all children (elements or entries) of the current collection level.

        Equivalent to calling ``all_children_with_filter`` with a constant
        ``true`` expression.  Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def all_children_with_filter(exp: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.CTX:
        r"""
        Select all children of the current collection level that satisfy ``exp``.

        The expression is evaluated per child element.  Use loop-variable
        expressions (``FilterExpression.int_loop_var``,
        ``FilterExpression.float_loop_var``,
        ``FilterExpression.string_loop_var``,
        ``FilterExpression.map_loop_var``,
        ``FilterExpression.bool_loop_var``) inside ``exp`` to reference the
        current element.  Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def map_keys_in(keys: typing.Sequence[typing.Any]) -> _aerospike_async_native.CTX:
        r"""
        Select map entries whose keys are in ``keys``.

        Requires Aerospike Server version >= 8.1.2.
        """
    @staticmethod
    def and_filter(exp: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.CTX:
        r"""
        AND-combine the previous filter context with ``exp``. Used to
        stack additional predicates onto an ``all_children_with_filter``
        step.

        Requires Aerospike Server version >= 8.1.2.
        """
    @staticmethod
    def to_base64(ctx: typing.Sequence[_aerospike_async_native.CTX]) -> builtins.str:
        r"""
        Encode a context array to base64 — pairs with :meth:`from_base64`.

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def from_base64(b64: builtins.str) -> builtins.list[_aerospike_async_native.CTX]:
        r"""
        Restore a context array from the base64-encoded form produced by
        the matching :meth:`to_base64` helper.

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def from_bytes(bytes: typing.Sequence[builtins.int]) -> builtins.list[_aerospike_async_native.CTX]:
        r"""
        Restore a context array from the raw byte stream that base64
        encodes.

        Requires Aerospike Server version >= 8.1.1.
        """

class CdtOperation:
    r"""
    CDT path expression operations for reading and writing nested CDT data.

    Requires Aerospike Server version >= 8.1.1.
    """
    @staticmethod
    def select_by_path(bin_name: builtins.str, flag: builtins.int, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.CdtOperation:
        r"""
        Create a CDT path select operation.

        Reads nested CDT data identified by the given path context, returning
        result elements controlled by ``flag``.

        Args:
            bin_name: Name of the bin containing the top-level CDT.
            flag: Controls what is returned. Pass any combination of
                ``SelectFlags`` constants (e.g. ``SelectFlags.VALUE |
                SelectFlags.NO_FAIL``) — the resulting value is a plain
                ``int``, matching JSDK's ``select_by_path(int flag)`` shape.
            ctx: Path into the CDT — one ``CTX`` per nesting level.

        Returns:
            A ``CdtOperation`` to pass to ``client.operate()``.

        Example::

            from aerospike_async import CdtOperation, CTX, SelectFlags
            op = CdtOperation.select_by_path(
                "inventory",
                SelectFlags.VALUE,
                [CTX.map_key("books"), CTX.list_index(0)],
            )
        """
    @staticmethod
    def modify_by_path(bin_name: builtins.str, flag: builtins.int, exp: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.CdtOperation:
        r"""
        Create a CDT path modify operation.

        Writes to nested CDT data identified by the given path context, using
        ``exp`` to compute the new value.

        Args:
            bin_name: Name of the bin containing the top-level CDT.
            flag: Controls error-handling behavior. Pass any combination of
                ``ModifyFlags`` constants — the resulting value is a plain
                ``int``, matching JSDK's ``modify_by_path(int flag)`` shape.
            exp: Expression that produces the value to write.
            ctx: Path into the CDT — one ``CTX`` per nesting level.

        Returns:
            A ``CdtOperation`` to pass to ``client.operate()``.

        Example::

            from aerospike_async import CdtOperation, CTX, ModifyFlags, FilterExpression
            price_path = [CTX.map_key("books"), CTX.list_rank(0), CTX.map_key("price")]
            new_price = FilterExpression.float_val(9.99)
            op = CdtOperation.modify_by_path(
                "inventory",
                ModifyFlags.DEFAULT,
                new_price,
                price_path,
            )
        """
    @staticmethod
    def select_values(bin_name: builtins.str, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.CdtOperation:
        r"""
        Select the *values* at every path-resolved location
        (``SelectFlags.VALUE``). Equivalent to
        ``CdtOperation.select_by_path(bin, SelectFlags.VALUE, ctx)``.

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def select_map_keys(bin_name: builtins.str, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.CdtOperation:
        r"""
        Select the matching *map keys* (``SelectFlags.MAP_KEY``).

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def select_map_entries(bin_name: builtins.str, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.CdtOperation:
        r"""
        Select map *key/value pairs* (``SelectFlags.MAP_KEY_VALUE``).

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def select_matching_tree(bin_name: builtins.str, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.CdtOperation:
        r"""
        Select the *original tree shape* preserving only matching nodes
        (``SelectFlags.MATCHING_TREE``).

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def modify(bin_name: builtins.str, exp: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.CdtOperation:
        r"""
        Modify with default flags, failing on type mismatches
        (``ModifyFlags.DEFAULT``).

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def modify_no_fail(bin_name: builtins.str, exp: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.CdtOperation:
        r"""
        Modify with ``ModifyFlags.NO_FAIL`` so type-mismatched leaves are
        silently skipped instead of aborting the whole operation.

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def remove(bin_name: builtins.str, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.CdtOperation:
        r"""
        Remove the leaves resolved by a path. Equivalent to
        ``CdtOperation.modify_by_path(bin, ModifyFlags.DEFAULT, FilterExpression.remove_result(), ctx)``.

        Requires Aerospike Server version >= 8.1.1.
        """

class Client:
    def __new__(cls) -> _aerospike_async_native.Client: ...
    def seeds(self) -> builtins.str: ...
    def is_strong_consistency(self, namespace: builtins.str) -> typing.Optional[builtins.bool]:
        r"""
        Returns whether ``namespace`` is configured for strong
        consistency on the cluster.

        Reads from the in-memory partition map (no network I/O). PSDK's
        :class:`Session` uses this internally; user code rarely needs it.

        Returns:
            ``Some(True)``: SC namespace.
            ``Some(False)``: AP namespace.
            ``None``: namespace not in the partition map (unknown
            namespace, or partition map not yet populated for this cluster).
        """
    def close(self) -> typing.Awaitable[typing.Any]:
        r"""
        Closes the connection to the Aerospike cluster.
        """
    def is_connected(self) -> typing.Awaitable[bool]:
        r"""
        Returns true if the client is connected to any cluster nodes.
        """
    def close_blocking(self) -> None:
        r"""
        Synchronously close the connection to the Aerospike cluster.
        """
    def is_connected_blocking(self) -> builtins.bool:
        r"""
        Synchronously check whether the client is connected to any cluster nodes.

        The underlying check is non-blocking, so this returns immediately
        without invoking the Tokio runtime.
        """
    def put_blocking(self, key: _aerospike_async_native.Key, bins: dict, *, policy: typing.Optional[_aerospike_async_native.WritePolicy] = None, policy_sc: typing.Optional[_aerospike_async_native.WritePolicy] = None, txn: typing.Optional[_aerospike_async_native.Txn] = None) -> None:
        r"""
        Synchronously write record bin(s).

        When `policy_sc` is provided, PAC resolves the key's namespace
        mode (AP vs SC) at op time via the cluster accessor and picks
        the matching base policy. The `txn` override is applied after
        the mode pick on a cloned policy so the caller's cached policies
        stay untouched across concurrent ops.
        """
    def get_blocking(self, key: _aerospike_async_native.Key, bins: typing.Optional[typing.Sequence[builtins.str]] = None, *, policy: typing.Optional[_aerospike_async_native.ReadPolicy] = None, policy_sc: typing.Optional[_aerospike_async_native.ReadPolicy] = None, filter_expression: typing.Optional[_aerospike_async_native.FilterExpression] = None, txn: typing.Optional[_aerospike_async_native.Txn] = None) -> _aerospike_async_native.Record:
        r"""
        Synchronously read a record for the specified key.

        When `policy_sc` is provided, PAC resolves the namespace mode at
        op time and picks AP vs SC. `filter_expression` / `txn` are
        applied after the mode pick on a cloned policy so the caller's
        cached policies stay untouched.
        """
    def delete_blocking(self, key: _aerospike_async_native.Key, *, policy: typing.Optional[_aerospike_async_native.WritePolicy] = None) -> builtins.bool:
        r"""
        Synchronously delete a record for the specified key.

        Returns ``True`` if the record existed on the server before the
        delete, ``False`` otherwise.
        """
    def commit_blocking(self, txn: _aerospike_async_native.Txn) -> _aerospike_async_native.CommitStatus:
        r"""
        Synchronously commit a multi-record transaction.
        """
    def abort_blocking(self, txn: _aerospike_async_native.Txn) -> _aerospike_async_native.AbortStatus:
        r"""
        Synchronously abort a multi-record transaction.
        """
    def add_blocking(self, key: _aerospike_async_native.Key, bins: typing.Mapping[builtins.str, typing.Any], *, policy: typing.Optional[_aerospike_async_native.WritePolicy] = None) -> None:
        r"""
        Synchronously add integer bin values.
        """
    def append_blocking(self, key: _aerospike_async_native.Key, bins: typing.Mapping[builtins.str, typing.Any], *, policy: typing.Optional[_aerospike_async_native.WritePolicy] = None) -> None:
        r"""
        Synchronously append string bin values.
        """
    def prepend_blocking(self, key: _aerospike_async_native.Key, bins: typing.Mapping[builtins.str, typing.Any], *, policy: typing.Optional[_aerospike_async_native.WritePolicy] = None) -> None:
        r"""
        Synchronously prepend string bin values.
        """
    def touch_blocking(self, key: _aerospike_async_native.Key, *, policy: typing.Optional[_aerospike_async_native.WritePolicy] = None) -> None:
        r"""
        Synchronously reset record TTL.
        """
    def exists_blocking(self, key: _aerospike_async_native.Key, *, policy: typing.Optional[_aerospike_async_native.ReadPolicy] = None) -> builtins.bool:
        r"""
        Synchronously check whether a record exists.
        """
    def operate_blocking(self, key: _aerospike_async_native.Key, operations: typing.Sequence[typing.Any], *, policy: typing.Optional[_aerospike_async_native.WritePolicy] = None, policy_sc: typing.Optional[_aerospike_async_native.WritePolicy] = None, record_exists_action: typing.Optional[_aerospike_async_native.RecordExistsAction] = None, expiration: typing.Optional[_aerospike_async_native.Expiration] = None, generation: typing.Optional[builtins.int] = None, durable_delete: typing.Optional[builtins.bool] = None, filter_expression: typing.Optional[_aerospike_async_native.FilterExpression] = None, txn: typing.Optional[_aerospike_async_native.Txn] = None) -> _aerospike_async_native.Record:
        r"""
        Synchronously execute multiple operations atomically on a single record.

        When `policy_sc` is provided, PAC resolves the namespace mode at
        op time and picks AP vs SC. The full override set is applied
        after the mode pick on a cloned policy so the caller's cached
        policies stay untouched across concurrent ops.
        """
    def execute_udf_blocking(self, key: _aerospike_async_native.Key, server_path: builtins.str, function_name: builtins.str, args: typing.Optional[typing.Sequence[typing.Any]] = None, *, policy: typing.Optional[_aerospike_async_native.WritePolicy] = None) -> typing.Optional[typing.Any]:
        r"""
        Synchronously execute a registered UDF on a single record.
        """
    def query_blocking(self, statement: _aerospike_async_native.Statement, partition_filter: _aerospike_async_native.PartitionFilter, *, policy: typing.Optional[_aerospike_async_native.QueryPolicy] = None) -> _aerospike_async_native.Recordset:
        r"""
        Synchronously execute a query and return a Recordset that supports
        `for record in recordset:` iteration via `__iter__`/`__next__`.
        """
    def query_explain_blocking(self, namespace: builtins.str, ael: builtins.str, *, set_name: typing.Optional[builtins.str] = None, index_name_hint: typing.Optional[builtins.str] = None, explain_where_flags: typing.Optional[builtins.int] = None, policy: typing.Optional[_aerospike_async_native.QueryPolicy] = None) -> _aerospike_async_native.QueryPlan:
        r"""
        Synchronously run phase 1 (explain) of server-led query selection.
        """
    def query_with_plan_blocking(self, statement: _aerospike_async_native.Statement, partition_filter: _aerospike_async_native.PartitionFilter, plan: _aerospike_async_native.QueryPlan, *, policy: typing.Optional[_aerospike_async_native.QueryPolicy] = None) -> _aerospike_async_native.Recordset:
        r"""
        Synchronously execute a partitioned query using a server query plan.
        """
    def query_operate_blocking(self, statement: _aerospike_async_native.Statement, operations: typing.Sequence[typing.Any], *, write_policy: typing.Optional[_aerospike_async_native.WritePolicy] = None) -> _aerospike_async_native.ExecuteTask:
        r"""
        Synchronously execute a background query that performs ops on each matching record.
        """
    def query_execute_udf_blocking(self, statement: _aerospike_async_native.Statement, package_name: builtins.str, function_name: builtins.str, args: typing.Optional[typing.Sequence[typing.Any]] = None, *, write_policy: typing.Optional[_aerospike_async_native.WritePolicy] = None) -> _aerospike_async_native.ExecuteTask:
        r"""
        Synchronously apply a UDF to records matching the statement (background).
        """
    def register_udf_blocking(self, udf_body: typing.Sequence[builtins.int], server_path: builtins.str, language: _aerospike_async_native.UDFLang, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> _aerospike_async_native.RegisterTask:
        r"""
        Synchronously register a UDF module from in-memory bytes.
        """
    def register_udf_from_file_blocking(self, client_path: builtins.str, server_path: builtins.str, language: _aerospike_async_native.UDFLang, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> _aerospike_async_native.RegisterTask:
        r"""
        Synchronously register a UDF module from a local file path.
        """
    def remove_udf_blocking(self, server_path: builtins.str, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> _aerospike_async_native.UdfRemoveTask:
        r"""
        Synchronously remove a registered UDF module.
        """
    def truncate_blocking(self, namespace: builtins.str, set_name: builtins.str, before_nanos: typing.Optional[builtins.int] = None, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> None:
        r"""
        Synchronously truncate records in a namespace/set.
        """
    def create_index_blocking(self, namespace: builtins.str, set_name: builtins.str, bin_name: builtins.str, index_name: builtins.str, index_type: _aerospike_async_native.IndexType, cit: typing.Optional[_aerospike_async_native.CollectionIndexType] = None, ctx: typing.Optional[typing.Sequence[_aerospike_async_native.CTX]] = None, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> None:
        r"""
        Synchronously create a secondary index on a bin.
        """
    def drop_index_blocking(self, namespace: builtins.str, set_name: builtins.str, index_name: builtins.str, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> _aerospike_async_native.DropIndexTask:
        r"""
        Synchronously drop a secondary index.
        """
    def info_blocking(self, command: builtins.str) -> builtins.dict[builtins.str, builtins.str]:
        r"""
        Synchronously execute an info command on a random cluster node.
        """
    def info_on_all_nodes_blocking(self, command: builtins.str) -> builtins.dict[builtins.str, builtins.dict[builtins.str, builtins.str]]:
        r"""
        Synchronously execute an info command on all cluster nodes.
        """
    def node_names_blocking(self) -> builtins.list[builtins.str]:
        r"""
        Synchronously list cluster node names.
        """
    def create_user_blocking(self, user: builtins.str, password: builtins.str, roles: typing.Sequence[builtins.str], *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> None:
        r"""
        Synchronously create a new user.
        """
    def create_pki_user_blocking(self, user: builtins.str, roles: typing.Sequence[builtins.str], *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> None:
        r"""
        Synchronously create a PKI user (TLS-cert auth).
        """
    def drop_user_blocking(self, user: builtins.str, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> None:
        r"""
        Synchronously remove a user.
        """
    def change_password_blocking(self, user: builtins.str, password: builtins.str, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> None:
        r"""
        Synchronously change a user's password.
        """
    def grant_roles_blocking(self, user: builtins.str, roles: typing.Sequence[builtins.str], *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> None:
        r"""
        Synchronously grant roles to a user.
        """
    def revoke_roles_blocking(self, user: builtins.str, roles: typing.Sequence[builtins.str], *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> None:
        r"""
        Synchronously revoke roles from a user.
        """
    def query_users_blocking(self, user: typing.Optional[builtins.str] = None, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> builtins.list[_aerospike_async_native.User]:
        r"""
        Synchronously query users and their roles.
        """
    def query_roles_blocking(self, role: typing.Optional[builtins.str] = None, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> builtins.list[_aerospike_async_native.Role]:
        r"""
        Synchronously query roles and their privileges.
        """
    def create_role_blocking(self, role_name: builtins.str, privileges: typing.Sequence[_aerospike_async_native.Privilege], allowlist: typing.Sequence[builtins.str], read_quota: builtins.int, write_quota: builtins.int, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> None:
        r"""
        Synchronously create a user-defined role.
        """
    def drop_role_blocking(self, role_name: builtins.str, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> None:
        r"""
        Synchronously remove a user-defined role.
        """
    def grant_privileges_blocking(self, role_name: builtins.str, privileges: typing.Sequence[_aerospike_async_native.Privilege], *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> None:
        r"""
        Synchronously grant privileges to a role.
        """
    def revoke_privileges_blocking(self, role_name: builtins.str, privileges: typing.Sequence[_aerospike_async_native.Privilege], *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> None:
        r"""
        Synchronously revoke privileges from a role.
        """
    def set_allowlist_blocking(self, role_name: builtins.str, allowlist: typing.Sequence[builtins.str], *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> None:
        r"""
        Synchronously set IP allowlist for a role.
        """
    def set_quotas_blocking(self, role_name: builtins.str, read_quota: builtins.int, write_quota: builtins.int, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> None:
        r"""
        Synchronously set per-second quotas for a role.
        """
    def create_index_using_expression_blocking(self, namespace: builtins.str, set_name: builtins.str, index_name: builtins.str, index_type: _aerospike_async_native.IndexType, expression: _aerospike_async_native.FilterExpression, cit: typing.Optional[_aerospike_async_native.CollectionIndexType] = None, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> _aerospike_async_native.IndexTask:
        r"""
        Synchronously create a secondary index using an expression.
        """
    def set_xdr_filter_blocking(self, datacenter: builtins.str, namespace: builtins.str, filter_expression: typing.Optional[_aerospike_async_native.FilterExpression] = None, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> None:
        r"""
        Synchronously set the XDR filter for a datacenter / namespace.
        """
    def get_node_blocking(self, name: builtins.str) -> _aerospike_async_native.Node:
        r"""
        Synchronously look up a single cluster node by name.
        """
    def nodes_blocking(self) -> builtins.list[_aerospike_async_native.Node]:
        r"""
        Synchronously list all active cluster nodes.
        """
    def batch_read_blocking(self, keys: typing.Sequence[_aerospike_async_native.Key], bins: typing.Optional[typing.Sequence[builtins.str]] = None, *, batch_policy: typing.Optional[_aerospike_async_native.BatchPolicy] = None, read_policy: typing.Optional[_aerospike_async_native.BatchReadPolicy] = None) -> builtins.list[_aerospike_async_native.BatchRecord]:
        r"""
        Synchronously read multiple records by key in one batch.
        """
    def batch_write_blocking(self, keys: typing.Sequence[_aerospike_async_native.Key], bins_list: typing.Sequence[typing.Any], *, batch_policy: typing.Optional[_aerospike_async_native.BatchPolicy] = None, write_policy: typing.Optional[_aerospike_async_native.BatchWritePolicy] = None) -> builtins.list[_aerospike_async_native.BatchRecord]:
        r"""
        Synchronously write multiple records by key in one batch.
        """
    def batch_operate_blocking(self, keys: typing.Sequence[_aerospike_async_native.Key], operations_list: typing.Sequence[typing.Sequence[typing.Any]], *, batch_policy: typing.Optional[_aerospike_async_native.BatchPolicy] = None, write_policy: typing.Optional[_aerospike_async_native.BatchWritePolicy] = None) -> builtins.list[_aerospike_async_native.BatchRecord]:
        r"""
        Synchronously perform per-key ops on multiple records in one batch.
        """
    def batch_delete_blocking(self, keys: typing.Sequence[_aerospike_async_native.Key], *, batch_policy: typing.Optional[_aerospike_async_native.BatchPolicy] = None, delete_policy: typing.Optional[_aerospike_async_native.BatchDeletePolicy] = None) -> builtins.list[_aerospike_async_native.BatchRecord]:
        r"""
        Synchronously delete multiple records by key in one batch.
        """
    def batch_exists_blocking(self, keys: typing.Sequence[_aerospike_async_native.Key], *, batch_policy: typing.Optional[_aerospike_async_native.BatchPolicy] = None, read_policy: typing.Optional[_aerospike_async_native.BatchReadPolicy] = None) -> builtins.list[builtins.bool]:
        r"""
        Synchronously check existence of multiple keys in one batch.
        """
    def batch_get_header_blocking(self, keys: typing.Sequence[_aerospike_async_native.Key], *, batch_policy: typing.Optional[_aerospike_async_native.BatchPolicy] = None, read_policy: typing.Optional[_aerospike_async_native.BatchReadPolicy] = None) -> builtins.list[typing.Optional[_aerospike_async_native.Record]]:
        r"""
        Synchronously read multiple record headers (metadata only) in one batch.
        """
    def batch_apply_blocking(self, keys: typing.Sequence[_aerospike_async_native.Key], udf_name: builtins.str, function_name: builtins.str, args: typing.Optional[typing.Sequence[typing.Any]], *, batch_policy: typing.Optional[_aerospike_async_native.BatchPolicy] = None, udf_policy: typing.Optional[_aerospike_async_native.BatchUDFPolicy] = None) -> builtins.list[_aerospike_async_native.BatchRecord]:
        r"""
        Synchronously apply a UDF to multiple keys in one batch.
        """
    def batch_blocking(self, ops: typing.Sequence[typing.Any], *, batch_policy: typing.Optional[_aerospike_async_native.BatchPolicy] = None) -> builtins.list[_aerospike_async_native.BatchRecord]:
        r"""
        Synchronously execute a mixed batch of read/write/delete ops.
        """
    def batch_stream_blocking(self, ops: typing.Sequence[typing.Any], *, batch_policy: typing.Optional[_aerospike_async_native.BatchPolicy] = None) -> _aerospike_async_native.BatchRecordStream:
        r"""
        Synchronously execute a streaming batch.

        Same shape as :meth:`batch_blocking` but returns a
        :class:`BatchRecordStream` whose items arrive in completion order
        (the node that responds first yields first). Each item is a
        `(idx, BatchRecord)` tuple. Cluster-level errors raise here;
        per-key errors land on each `BatchRecord`'s `result_code`.
        """
    def put(self, key: _aerospike_async_native.Key, bins: dict, *, policy: typing.Optional[_aerospike_async_native.WritePolicy] = None, policy_sc: typing.Optional[_aerospike_async_native.WritePolicy] = None, txn: typing.Optional[_aerospike_async_native.Txn] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Write record bin(s).

        When `policy_sc` is provided, PAC resolves the key's namespace
        mode at op time and picks AP vs SC. The `txn` override is applied
        after the mode pick on a cloned policy so caller-cached policies
        stay untouched across concurrent ops.
        """
    def get(self, key: _aerospike_async_native.Key, bins: typing.Optional[typing.Sequence[builtins.str]] = None, *, policy: typing.Optional[_aerospike_async_native.ReadPolicy] = None, policy_sc: typing.Optional[_aerospike_async_native.ReadPolicy] = None, filter_expression: typing.Optional[_aerospike_async_native.FilterExpression] = None, txn: typing.Optional[_aerospike_async_native.Txn] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Read record for the specified key.

        When `policy_sc` is provided, PAC resolves the namespace mode at
        op time and picks AP vs SC. `filter_expression` / `txn` are
        applied after the mode pick on a cloned policy.
        """
    def operate(self, key: _aerospike_async_native.Key, operations: typing.Sequence[typing.Any], *, policy: typing.Optional[_aerospike_async_native.WritePolicy] = None, policy_sc: typing.Optional[_aerospike_async_native.WritePolicy] = None, record_exists_action: typing.Optional[_aerospike_async_native.RecordExistsAction] = None, expiration: typing.Optional[_aerospike_async_native.Expiration] = None, generation: typing.Optional[builtins.int] = None, durable_delete: typing.Optional[builtins.bool] = None, filter_expression: typing.Optional[_aerospike_async_native.FilterExpression] = None, txn: typing.Optional[_aerospike_async_native.Txn] = None) -> typing.Awaitable[Record]:
        r"""
        Execute multiple operations atomically on a single record.

        When `policy_sc` is provided, PAC resolves the namespace mode at
        op time and picks AP vs SC. The full override set is applied
        after the mode pick on a cloned policy.
        """
    def add(self, key: _aerospike_async_native.Key, bins: typing.Mapping[builtins.str, typing.Any], *, policy: typing.Optional[_aerospike_async_native.WritePolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Add integer bin values to existing record bin values. The policy specifies the transaction
        timeout, record expiration and how the transaction is handled when the record already
        exists. This call only works for integer values.
        """
    def append(self, key: _aerospike_async_native.Key, bins: typing.Mapping[builtins.str, typing.Any], *, policy: typing.Optional[_aerospike_async_native.WritePolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Append bin string values to existing record bin values. The policy specifies the
        transaction timeout, record expiration and how the transaction is handled when the record
        already exists. This call only works for string values.
        """
    def prepend(self, key: _aerospike_async_native.Key, bins: typing.Mapping[builtins.str, typing.Any], *, policy: typing.Optional[_aerospike_async_native.WritePolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Prepend bin string values to existing record bin values. The policy specifies the
        transaction timeout, record expiration and how the transaction is handled when the record
        already exists. This call only works for string values.
        """
    def delete(self, key: _aerospike_async_native.Key, *, policy: typing.Optional[_aerospike_async_native.WritePolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Delete record for specified key. The policy specifies the transaction timeout.
        The call returns `true` if the record existed on the server before deletion.
        """
    def touch(self, key: _aerospike_async_native.Key, *, policy: typing.Optional[_aerospike_async_native.WritePolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Reset record's time to expiration using the policy's expiration. Fail if the record does
        not exist.
        """
    def batch_read(self, keys: typing.Sequence[_aerospike_async_native.Key], bins: typing.Optional[typing.Sequence[builtins.str]] = None, *, batch_policy: typing.Optional[_aerospike_async_native.BatchPolicy] = None, read_policy: typing.Optional[_aerospike_async_native.BatchReadPolicy] = None) -> typing.Awaitable[typing.List[BatchRecord]]:
        r"""
        Read multiple records for specified keys in one batch call.
        """
    def batch_write(self, keys: typing.Sequence[_aerospike_async_native.Key], bins_list: typing.Sequence[typing.Any], *, batch_policy: typing.Optional[_aerospike_async_native.BatchPolicy] = None, write_policy: typing.Optional[_aerospike_async_native.BatchWritePolicy] = None) -> typing.Awaitable[typing.List[BatchRecord]]:
        r"""
        Write multiple records for specified keys in one batch call.
        """
    def batch_operate(self, keys: typing.Sequence[_aerospike_async_native.Key], operations_list: typing.Sequence[typing.Sequence[typing.Any]], *, batch_policy: typing.Optional[_aerospike_async_native.BatchPolicy] = None, write_policy: typing.Optional[_aerospike_async_native.BatchWritePolicy] = None) -> typing.Awaitable[typing.List[BatchRecord]]:
        r"""
        Perform read/write operations on multiple keys in one batch call.
        """
    def batch_delete(self, keys: typing.Sequence[_aerospike_async_native.Key], *, batch_policy: typing.Optional[_aerospike_async_native.BatchPolicy] = None, delete_policy: typing.Optional[_aerospike_async_native.BatchDeletePolicy] = None) -> typing.Awaitable[typing.List[BatchRecord]]:
        r"""
        Delete multiple records for specified keys in one batch call.
        """
    def batch_exists(self, keys: typing.Sequence[_aerospike_async_native.Key], *, batch_policy: typing.Optional[_aerospike_async_native.BatchPolicy] = None, read_policy: typing.Optional[_aerospike_async_native.BatchReadPolicy] = None) -> typing.Awaitable[typing.List[builtins.bool]]:
        r"""
        Check if multiple record keys exist in one batch call.
        """
    def batch_get_header(self, keys: typing.Sequence[_aerospike_async_native.Key], *, batch_policy: typing.Optional[_aerospike_async_native.BatchPolicy] = None, read_policy: typing.Optional[_aerospike_async_native.BatchReadPolicy] = None) -> typing.Awaitable[typing.List[typing.Optional[Record]]]:
        r"""
        Read multiple record headers (metadata only, no bin data) for specified keys in one batch call.
        """
    def batch_apply(self, keys: typing.Sequence[_aerospike_async_native.Key], udf_name: builtins.str, function_name: builtins.str, args: typing.Optional[typing.Sequence[typing.Any]], *, batch_policy: typing.Optional[_aerospike_async_native.BatchPolicy] = None, udf_policy: typing.Optional[_aerospike_async_native.BatchUDFPolicy] = None) -> typing.Awaitable[typing.List[BatchRecord]]:
        r"""
        Apply UDF operations on multiple keys in one batch call.
        """
    def batch(self, ops: typing.Sequence[typing.Any], *, batch_policy: typing.Optional[_aerospike_async_native.BatchPolicy] = None) -> typing.Awaitable[typing.Sequence[BatchRecord]]:
        r"""
        Execute a mixed batch of read, write, and delete operations in a single server call.

        Each operation is specified via :class:`BatchReadOp`, :class:`BatchWriteOp`,
        or :class:`BatchDeleteOp`, each carrying its own key and per-record policy.

        Args:
            batch_policy: Optional :class:`BatchPolicy` for the entire batch.
            ops: List of :class:`BatchReadOp`, :class:`BatchWriteOp`, and/or
                 :class:`BatchDeleteOp` objects.

        Returns:
            A list of :class:`BatchRecord` results in the same order as the input ops.
        """
    def batch_stream(self, ops: typing.Sequence[typing.Any], *, batch_policy: typing.Optional[_aerospike_async_native.BatchPolicy] = None) -> typing.Awaitable[BatchRecordStream]:
        r"""
        Asynchronously execute a streaming batch.

        Returns an awaitable resolving to a :class:`BatchRecordStream`.
        Items arrive in completion order (the node that responds first
        yields first). Each item is a `(idx, BatchRecord)` tuple where
        ``idx`` is the position of the originating op in ``ops``.
        Per-key errors land on each `BatchRecord`'s ``result_code``;
        cluster-level errors raise from the resolving awaitable or
        from ``__anext__``.
        """
    def execute_udf(self, key: _aerospike_async_native.Key, server_path: builtins.str, function_name: builtins.str, args: typing.Optional[typing.Sequence[typing.Any]] = None, *, policy: typing.Optional[_aerospike_async_native.WritePolicy] = None) -> typing.Awaitable[typing.Optional[typing.Any]]:
        r"""
        Execute a UDF (User Defined Function) on a single record.

        Args:
            policy: WritePolicy for the operation.
            key: The key of the record to execute the UDF on.
            server_path: Server path to the UDF module (e.g., "example.lua").
            function_name: Name of the function to execute within the UDF module.
            args: Optional list of arguments to pass to the UDF function.

        Returns:
            Optional Value containing the UDF result, or None if the UDF returns no value.
        """
    def query_operate(self, statement: _aerospike_async_native.Statement, operations: typing.Sequence[typing.Any], *, write_policy: typing.Optional[_aerospike_async_native.WritePolicy] = None) -> typing.Awaitable[ExecuteTask]:
        r"""
        Execute a query/scan and apply write operations to matching records (background job).
        Returns an ExecuteTask to poll for completion. Supports scalar and expression write
        operations (put, add, delete, touch, append, prepend, ExpOperation.write).
        List/map/bit/HLL operations are not supported for background query.

        Args:
            write_policy: WritePolicy for the background operation.
            statement: Statement (namespace, set, optional filters).
            operations: List of Operation objects (e.g. Operation.put, Operation.add, Operation.delete, Operation.touch).

        Returns:
            ExecuteTask to monitor completion (query_status, wait_till_complete).
        """
    def query_execute_udf(self, statement: _aerospike_async_native.Statement, package_name: builtins.str, function_name: builtins.str, args: typing.Optional[typing.Sequence[typing.Any]] = None, *, write_policy: typing.Optional[_aerospike_async_native.WritePolicy] = None) -> typing.Awaitable[ExecuteTask]:
        r"""
        Apply a UDF to records matching the statement filter (background job).
        Returns an ExecuteTask to poll for completion. Records are not returned.
        If the statement has no filter, the UDF is applied to all records in the namespace/set.

        Args:
            write_policy: WritePolicy for the background operation.
            statement: Statement (namespace, set, optional filters).
            package_name: Server-side UDF package name.
            function_name: UDF function to invoke.
            args: Optional arguments to pass to the UDF.

        Returns:
            ExecuteTask to monitor completion (query_status, wait_till_complete).
        """
    def register_udf(self, udf_body: typing.Sequence[builtins.int], server_path: builtins.str, language: _aerospike_async_native.UDFLang, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[RegisterTask]:
        r"""
        Register a UDF (User Defined Function) module on the server from bytes.

        Args:
            policy: AdminPolicy for the operation.
            udf_body: The UDF module content as bytes.
            server_path: Server path where the UDF will be stored (e.g., "example.lua").
            language: UDF language (UDFLang.LUA).

        Returns:
            RegisterTask that can be used to wait for registration completion.
        """
    def register_udf_from_file(self, client_path: builtins.str, server_path: builtins.str, language: _aerospike_async_native.UDFLang, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[RegisterTask]:
        r"""
        Register a UDF (User Defined Function) module on the server from a file.

        Args:
            policy: AdminPolicy for the operation.
            client_path: Local file path to the UDF module.
            server_path: Server path where the UDF will be stored (e.g., "example.lua").
            language: UDF language (UDFLang.LUA).

        Returns:
            RegisterTask that can be used to wait for registration completion.
        """
    def remove_udf(self, server_path: builtins.str, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[UdfRemoveTask]:
        r"""
        Remove a UDF (User Defined Function) module from the server.

        Args:
            policy: AdminPolicy for the operation.
            server_path: Server path to the UDF module to remove (e.g., "example.lua").

        Returns:
            UdfRemoveTask that can be used to wait for removal completion.
        """
    def exists(self, key: _aerospike_async_native.Key, *, policy: typing.Optional[_aerospike_async_native.ReadPolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Determine if a record key exists. The policy can be used to specify timeouts.
        """
    def exists_legacy(self, key: _aerospike_async_native.Key, *, policy: typing.Optional[_aerospike_async_native.ReadPolicy] = None) -> typing.Awaitable[typing.Tuple[Key, typing.Optional[typing.Any]]]:
        r"""
        Determine if a record key exists (legacy contract). Returns (key, meta) where meta=None if record not found.
        This matches the legacy Python client contract.
        """
    def truncate(self, namespace: builtins.str, set_name: builtins.str, before_nanos: typing.Optional[builtins.int] = None, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Removes all records in the specified namespace/set efficiently.
        """
    def create_index(self, namespace: builtins.str, set_name: builtins.str, bin_name: builtins.str, index_name: builtins.str, index_type: _aerospike_async_native.IndexType, cit: typing.Optional[_aerospike_async_native.CollectionIndexType] = None, ctx: typing.Optional[typing.Sequence[_aerospike_async_native.CTX]] = None, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Create a secondary index on a bin containing scalar values. This asynchronous server call
        returns before the command is complete.
        """
    def drop_index(self, namespace: builtins.str, set_name: builtins.str, index_name: builtins.str, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[DropIndexTask]:
        r"""
        Drop a secondary index. Returns a DropIndexTask to track completion.
        """
    def create_index_using_expression(self, namespace: builtins.str, set_name: builtins.str, index_name: builtins.str, index_type: _aerospike_async_native.IndexType, expression: _aerospike_async_native.FilterExpression, cit: typing.Optional[_aerospike_async_native.CollectionIndexType] = None, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[IndexTask]:
        r"""
        Create a secondary index using an expression. Returns an IndexTask to wait for completion.
        """
    def query(self, statement: _aerospike_async_native.Statement, partition_filter: _aerospike_async_native.PartitionFilter, *, policy: typing.Optional[_aerospike_async_native.QueryPolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Execute a query on all server nodes and return a record iterator. The query executor puts
        records on a queue in separate threads. The calling thread concurrently pops records off
        the queue through the record iterator.
        """
    def query_explain(self, namespace: builtins.str, ael: builtins.str, *, set_name: typing.Optional[builtins.str] = None, index_name_hint: typing.Optional[builtins.str] = None, explain_where_flags: typing.Optional[builtins.int] = None, policy: typing.Optional[_aerospike_async_native.QueryPolicy] = None) -> typing.Awaitable[QueryPlan]:
        r"""
        Run phase 1 (explain) of server-led query selection.
        """
    def query_with_plan(self, statement: _aerospike_async_native.Statement, partition_filter: _aerospike_async_native.PartitionFilter, plan: _aerospike_async_native.QueryPlan, *, policy: typing.Optional[_aerospike_async_native.QueryPolicy] = None) -> typing.Awaitable[Recordset]:
        r"""
        Execute a partitioned query using a server query plan from
        :meth:`query_explain`.
        """
    def create_user(self, user: builtins.str, password: builtins.str, roles: typing.Sequence[builtins.str], *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Creates a new user with password and roles. Clear-text password will be hashed using bcrypt
        before sending to server.
        """
    def create_pki_user(self, user: builtins.str, roles: typing.Sequence[builtins.str], *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Creates a PKI user with roles. PKI users authenticate via TLS client certificate (no password).
        Supported by Aerospike Server v8.1+ Enterprise.
        """
    def drop_user(self, user: builtins.str, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Removes a user from the cluster.
        """
    def change_password(self, user: builtins.str, password: builtins.str, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Changes a user's password. Clear-text password will be hashed using bcrypt before sending to server.
        """
    def grant_roles(self, user: builtins.str, roles: typing.Sequence[builtins.str], *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Adds roles to user's list of roles.
        """
    def revoke_roles(self, user: builtins.str, roles: typing.Sequence[builtins.str], *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Removes roles from user's list of roles.
        """
    def query_users(self, user: typing.Optional[builtins.str] = None, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Retrieves users and their roles.
        If None is passed for the user argument, all users will be returned.
        """
    def query_roles(self, role: typing.Optional[builtins.str] = None, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Retrieves roles and their privileges.
        If None is passed for the role argument, all roles will be returned.
        """
    def create_role(self, role_name: builtins.str, privileges: typing.Sequence[_aerospike_async_native.Privilege], allowlist: typing.Sequence[builtins.str], read_quota: builtins.int, write_quota: builtins.int, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Creates a user-defined role.
        Quotas require server security configuration "enable-quotas" to be set to true.
        Pass 0 for quota values for no limit.
        """
    def drop_role(self, role_name: builtins.str, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Removes a user-defined role.
        """
    def grant_privileges(self, role_name: builtins.str, privileges: typing.Sequence[_aerospike_async_native.Privilege], *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Grants privileges to a user-defined role.
        """
    def revoke_privileges(self, role_name: builtins.str, privileges: typing.Sequence[_aerospike_async_native.Privilege], *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Revokes privileges from a user-defined role.
        """
    def set_allowlist(self, role_name: builtins.str, allowlist: typing.Sequence[builtins.str], *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Sets IP address allowlist for a role.
        If allowlist is nil or empty, it removes existing allowlist from role.
        """
    def set_quotas(self, role_name: builtins.str, read_quota: builtins.int, write_quota: builtins.int, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Sets maximum reads/writes per second limits for a role.
        If a quota is zero, the limit is removed.
        Quotas require server security configuration "enable-quotas" to be set to true.
        Pass 0 for quota values for no limit.
        """
    def __str__(self) -> builtins.str: ...
    def __repr__(self) -> builtins.str: ...
    def __copy__(self) -> _aerospike_async_native.Client: ...
    def __deepcopy__(self, _memo: dict) -> _aerospike_async_native.Client: ...
    def node_names(self) -> typing.Awaitable[typing.List[str]]:
        r"""
        Returns a list of the names of the active server nodes in the cluster.
        """
    def get_node(self, name: builtins.str) -> typing.Awaitable[Node]:
        r"""
        Return node given its name.
        """
    def nodes(self) -> typing.Awaitable[typing.List[Node]]:
        r"""
        Returns a list of all active server nodes in the cluster.
        """
    def commit(self, txn: _aerospike_async_native.Txn) -> typing.Awaitable[CommitStatus]:
        r"""
        Commit a multi-record transaction.

        Verifies all transaction record versions, then applies all writes
        atomically. Returns a :class:`CommitStatus` indicating the outcome.
        Raises :exc:`aerospike_async.exceptions.CommitFailedError` if the
        commit fails part-way through.

        Args:
            txn: The transaction to commit.

        Returns:
            CommitStatus: The outcome of the commit.

        Raises:
            CommitFailedError: If the transaction could not be committed.

        Example::

            status = await client.commit(txn)
            assert status == CommitStatus.OK
        """
    def abort(self, txn: _aerospike_async_native.Txn) -> typing.Awaitable[AbortStatus]:
        r"""
        Abort a multi-record transaction, rolling back all writes.

        Args:
            txn: The transaction to abort.

        Returns:
            AbortStatus: The outcome of the abort.

        Raises:
            AerospikeError: If the abort itself encounters a server error.

        Example::

            status = await client.abort(txn)
            assert status == AbortStatus.OK
        """
    def info(self, command: builtins.str) -> typing.Awaitable[typing.Dict[str, str]]:
        r"""
        Execute an info command on a random cluster node.

        Args:
            command: The info command to execute (e.g., "namespaces", "statistics", "build").

        Returns:
            A dictionary containing the info command response as key-value pairs.
        """
    def info_on_all_nodes(self, command: builtins.str) -> typing.Awaitable[typing.Dict[str, typing.Dict[str, str]]]:
        r"""
        Execute an info command on all cluster nodes.

        Args:
            command: The info command to execute (e.g., "namespaces", "statistics", "build").

        Returns:
            A dictionary mapping node names to their info command responses.
        """
    def set_xdr_filter(self, datacenter: builtins.str, namespace: builtins.str, filter_expression: typing.Optional[_aerospike_async_native.FilterExpression] = None, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[typing.Any]:
        r"""
        Sets XDR filter for given datacenter and namespace. Pass None as filter to remove.
        """

class ClientPolicy:
    @property
    def per_client_runtime_workers(self) -> typing.Optional[builtins.int]:
        r"""
        Get the per-Client Tokio runtime worker count, if set.

        See :attr:`set_per_client_runtime_workers` for semantics.
        """
    @per_client_runtime_workers.setter
    def per_client_runtime_workers(self, value: typing.Optional[builtins.int]) -> None:
        r"""
        Per-Client Tokio runtime (opt-in). With ``Some(N)`` where
        ``N >= 1``, this Client gets a dedicated multi-thread runtime
        with ``N`` workers instead of the shared global one.
        ``None`` or ``Some(0)`` keeps the global runtime (default).

        Use this when multiple Clients on multiple async event loops
        coexist in one process: the global runtime's work-stealing
        collapses past ~4 concurrent loops on an 8-CPU host, while a
        dedicated per-Client runtime isolates the scheduling.

        Sizing: pick ``workers >= concurrency / 8`` for reasonable
        tail latency. Under-provisioning starves concurrent ops
        (32 in-flight tasks on 2 workers showed ~1000ms p99.9).
        """
    @property
    def user(self) -> typing.Optional[builtins.str]: ...
    @user.setter
    def user(self, value: typing.Optional[builtins.str]) -> None: ...
    @property
    def password(self) -> typing.Optional[builtins.str]: ...
    @password.setter
    def password(self, value: typing.Optional[builtins.str]) -> None: ...
    @property
    def auth_mode(self) -> _aerospike_async_native.AuthMode:
        r"""
        Get the current authentication mode.
        """
    @property
    def timeout(self) -> builtins.int: ...
    @timeout.setter
    def timeout(self, value: builtins.int) -> None: ...
    @property
    def idle_timeout(self) -> builtins.int:
        r"""
        Connection idle timeout. Every time a connection is used, its idle
        deadline will be extended by this duration. When this deadline is reached,
        the connection will be closed and discarded from the connection pool.
        """
    @idle_timeout.setter
    def idle_timeout(self, value: builtins.int) -> None: ...
    @property
    def min_conns_per_node(self) -> builtins.int:
        r"""
        Minimum number of connections allowed per server node. The client
        periodically allocates new connections if the total count (idle +
        in-flight) falls below this value.

        Server ``proto-fd-idle-ms`` may also need to be increased
        substantially if min connections are defined. The default directs
        the server to close connections idle for 60 seconds which can
        defeat the purpose of keeping connections in reserve.

        Default: ``0`` (disabled).
        """
    @min_conns_per_node.setter
    def min_conns_per_node(self, value: builtins.int) -> None: ...
    @property
    def max_conns_per_node(self) -> builtins.int: ...
    @max_conns_per_node.setter
    def max_conns_per_node(self, value: builtins.int) -> None: ...
    @property
    def conn_pools_per_node(self) -> builtins.int:
        r"""
        Number of connection pools used for each node. Machines with 8 CPU cores or less usually
        need only one connection pool per node. Machines with larger number of CPU cores may have
        their performance limited by contention for pooled connections. Contention for pooled
        connections can be reduced by creating multiple mini connection pools per node.
        """
    @conn_pools_per_node.setter
    def conn_pools_per_node(self, value: builtins.int) -> None: ...
    @property
    def use_services_alternate(self) -> builtins.bool:
        r"""
        UseServicesAlternate determines if the client should use "services-alternate"
        instead of "services" in info request during cluster tending.
        "services-alternate" returns server configured external IP addresses that client
        uses to talk to nodes.  "services-alternate" can be used in place of
        providing a client "ipMap".
        This feature is recommended instead of using the client-side IpMap above.

        "services-alternate" is available with Aerospike Server versions >= 3.7.1.
        """
    @use_services_alternate.setter
    def use_services_alternate(self, value: builtins.bool) -> None: ...
    @property
    def rack_ids(self) -> typing.Optional[builtins.list[builtins.int]]:
        r"""
        Mark this client as belonging to a rack, and track server rack data.  This field is useful when directing read commands to
        the server node that contains the key and exists on the same rack as the client.
        This serves to lower cloud provider costs when nodes are distributed across different
        racks/data centers.

        Replica.PreferRack and server rack configuration must
        also be set to enable this functionality.
        """
    @rack_ids.setter
    def rack_ids(self, value: typing.Optional[typing.Sequence[builtins.int]]) -> None: ...
    @property
    def fail_if_not_connected(self) -> builtins.bool:
        r"""
        Throw exception if host connection fails during addHost().
        """
    @fail_if_not_connected.setter
    def fail_if_not_connected(self, value: builtins.bool) -> None: ...
    @property
    def seed_only_cluster(self) -> builtins.bool:
        r"""
        **Testing-only.** When ``True``, the client never performs peer
        discovery or background tend: every op routes through the seed
        addresses given at construction. No tend task is spawned. Useful
        for benchmarking and unit-test setup where eliminating tend
        noise matters more than topology safety.

        **Do not enable in production.** With ``seed_only_cluster`` set,
        node restarts, master failover, and rebalances are invisible to
        the client — ops to a stale or down node will fail or hang.
        Default: ``False``.
        """
    @seed_only_cluster.setter
    def seed_only_cluster(self, value: builtins.bool) -> None: ...
    @property
    def buffer_reclaim_threshold(self) -> builtins.int:
        r"""
        Threshold at which the buffer attached to the connection will be shrunk by deallocating
        memory instead of just resetting the size of the underlying vec.
        Should be set to a value that covers as large a percentile of payload sizes as possible,
        while also being small enough not to occupy a significant amount of memory for the life
        of the connection pool.
        """
    @buffer_reclaim_threshold.setter
    def buffer_reclaim_threshold(self, value: builtins.int) -> None: ...
    @property
    def max_error_rate(self) -> builtins.int:
        r"""
        Maximum number of errors (network errors plus server-side ``TIMEOUT``,
        ``DEVICE_OVERLOAD``, ``KEY_BUSY``) tolerated against a single node within
        one ``error_rate_window``. Once exceeded, the client trips a per-node
        circuit breaker and rejects further commands targeted at that node with
        a ``MaxErrorRate`` exception until the next window resets. Set to ``0``
        to disable. Default: ``100``.
        """
    @max_error_rate.setter
    def max_error_rate(self, value: builtins.int) -> None: ...
    @property
    def error_rate_window(self) -> builtins.int:
        r"""
        Number of cluster tend iterations after which each node's error counter
        is reset. Smaller values make the circuit breaker more aggressive,
        larger values more lenient. Default: ``1``.
        """
    @error_rate_window.setter
    def error_rate_window(self, value: builtins.int) -> None: ...
    @property
    def tend_interval(self) -> builtins.int:
        r"""
        TendInterval determines interval for checking for cluster state changes.
        Minimum possible interval is 10 Milliseconds.
        """
    @tend_interval.setter
    def tend_interval(self, value: builtins.int) -> None: ...
    @property
    def ip_map(self) -> typing.Any:
        r"""
        A IP translation table is used in cases where different clients
        use different server IP addresses.  This may be necessary when
        using clients from both inside and outside a local area
        network. Default is no translation.
        The key is the IP address returned from friend info requests to other servers.
        The value is the real IP address used to connect to the server.
        """
    @ip_map.setter
    def ip_map(self, value: typing.Optional[dict]) -> None: ...
    @property
    def cluster_name(self) -> typing.Optional[builtins.str]:
        r"""
        Expected cluster name. It not `None`, server nodes must return this cluster name in order
        to join the client's view of the cluster. Should only be set when connecting to servers
        that support the "cluster-name" info command.
        """
    @cluster_name.setter
    def cluster_name(self, value: typing.Optional[builtins.str]) -> None: ...
    @property
    def application_id(self) -> typing.Optional[builtins.str]:
        r"""
        Identifies the application so that client operations can be
        correlated with server-side metrics. Default: ``None``.
        """
    @application_id.setter
    def application_id(self, value: typing.Optional[builtins.str]) -> None: ...
    @property
    def custom_client_id(self) -> typing.Optional[builtins.str]:
        r"""
        Override the ``client_id`` portion of the ``user_agent_id``
        payload sent to each node on connection validation. Intended for
        wrapper clients that embed the Rust core; end-user code should
        leave this as ``None``. Default: ``None``.
        """
    @custom_client_id.setter
    def custom_client_id(self, value: typing.Optional[builtins.str]) -> None: ...
    @property
    def tls_config(self) -> typing.Optional[_aerospike_async_native.TlsConfig]:
        r"""
        TLS configuration for secure connections.
        Set to None to disable TLS, or use TlsConfig to configure TLS.
        """
    @tls_config.setter
    def tls_config(self, value: typing.Optional[_aerospike_async_native.TlsConfig]) -> None: ...
    def __new__(cls) -> _aerospike_async_native.ClientPolicy: ...
    def set_auth_mode(self, mode: _aerospike_async_native.AuthMode, user: typing.Optional[builtins.str] = None, password: typing.Optional[builtins.str] = None) -> None:
        r"""
        Set the authentication mode.

        Args:
            mode: The authentication mode (AuthMode.NONE, AuthMode.INTERNAL, AuthMode.EXTERNAL, or AuthMode.PKI)
            user: Optional username (required for INTERNAL and EXTERNAL modes)
            password: Optional password (required for INTERNAL and EXTERNAL modes)

        Note: For PKI mode, user and password are ignored. TLS with client certificate is required.
        """
    def set_pki_auth(self) -> None:
        r"""
        Set authentication mode to PKI (certificate-based authentication).

        This requires TLS to be configured with a client certificate.
        Requires server version 5.7.0+.
        """
    def __str__(self) -> builtins.str: ...
    def __repr__(self) -> builtins.str: ...
    def __copy__(self) -> _aerospike_async_native.ClientPolicy: ...
    def __deepcopy__(self, _memo: dict) -> _aerospike_async_native.ClientPolicy: ...

class DropIndexTask:
    def query_status(self) -> typing.Awaitable[TaskStatus]: ...
    def wait_till_complete(self, sleep_time: builtins.float = 0.25, max_attempts: builtins.int = 80) -> typing.Awaitable[bool]: ...
    def query_status_blocking(self) -> _aerospike_async_native.TaskStatus:
        r"""
        Blocking sibling of :meth:`query_status` — returns the current
        status without spinning up an asyncio loop. Rejects calls made
        from inside a running asyncio loop.
        """
    def wait_till_complete_blocking(self, sleep_time: builtins.float = 0.25, max_attempts: builtins.int = 80) -> builtins.bool:
        r"""
        Blocking sibling of :meth:`wait_till_complete` — polls status
        until COMPLETE or NOT_FOUND without needing an asyncio loop.
        Rejects calls made from inside a running asyncio loop.

        Args:
            sleep_time: Time to sleep between status checks (seconds). Default: 0.25
            max_attempts: Maximum number of attempts before giving up. Default: 80 (20 seconds)

        Returns:
            True if task completed, False if max attempts reached
        """

class ExecuteTask:
    def query_status(self) -> typing.Awaitable[TaskStatus]: ...
    def wait_till_complete(self, sleep_time: builtins.float = 0.25, max_attempts: builtins.int = 80) -> typing.Awaitable[bool]: ...
    def query_status_blocking(self) -> _aerospike_async_native.TaskStatus:
        r"""
        Blocking sibling of :meth:`query_status` — returns the current
        status without spinning up an asyncio loop. Rejects calls made
        from inside a running asyncio loop.
        """
    def wait_till_complete_blocking(self, sleep_time: builtins.float = 0.25, max_attempts: builtins.int = 80) -> builtins.bool:
        r"""
        Blocking sibling of :meth:`wait_till_complete` — polls status
        until COMPLETE or NOT_FOUND without needing an asyncio loop.
        Rejects calls made from inside a running asyncio loop.

        Args:
            sleep_time: Time to sleep between status checks (seconds). Default: 0.25
            max_attempts: Maximum number of attempts before giving up. Default: 80 (20 seconds)

        Returns:
            True if task completed, False if max attempts reached
        """

class ExpOperation:
    r"""
    Expression operations. Create expression operations used by the client's `operate()` method.
    Expression operations allow evaluating expressions on the server and optionally storing
    the result in a bin.
    """
    @staticmethod
    def read(name: builtins.str, exp: _aerospike_async_native.FilterExpression, flags: typing.Optional[typing.Any] = None) -> _aerospike_async_native.ExpOperation:
        r"""
        Create expression read operation.

        Evaluates the expression and returns the result. The result is returned
        in the record bins with the specified name.

        Args:
            name: Name to assign to the expression result in the returned record.
            exp: Expression to evaluate.
            flags: Expression read flags (default: ExpReadFlags.DEFAULT). May
                be an ``ExpReadFlags`` member, a combined bitmask
                (``ExpReadFlags.X | ExpReadFlags.Y``), or a raw ``int``.

        Returns:
            An ExpOperation to use with client.operate().
        """
    @staticmethod
    def write(bin_name: builtins.str, exp: _aerospike_async_native.FilterExpression, flags: typing.Optional[typing.Any] = None) -> _aerospike_async_native.ExpOperation:
        r"""
        Create expression write operation.

        Evaluates the expression and writes the result to the specified bin.

        Args:
            bin_name: Name of bin to store expression result.
            exp: Expression to evaluate.
            flags: Expression write flags (default: ExpWriteFlags.DEFAULT). May
                be an ``ExpWriteFlags`` member, a combined bitmask
                (``ExpWriteFlags.X | ExpWriteFlags.Y``), or a raw ``int``.

        Returns:
            An ExpOperation to use with client.operate().
        """

class Expiration:
    NAMESPACE_DEFAULT: _aerospike_async_native.Expiration = ...
    NEVER_EXPIRE: _aerospike_async_native.Expiration = ...
    DONT_UPDATE: _aerospike_async_native.Expiration = ...
    @staticmethod
    def seconds(s: builtins.int) -> _aerospike_async_native.Expiration: ...
    def __richcmp__(self, other: _aerospike_async_native.Expiration, op: int) -> builtins.bool: ...
    def __hash__(self) -> builtins.int: ...

@typing.final
class FastRng:
    r"""
    Fast PRNG (xoshiro256++ via rand crate) exposed for Python callers
    that want sub-100 ns random number generation per call. CPython's
    stdlib `random.Random` uses Mersenne Twister at ~700 ns/call —
    fine for general use, but a measurable handicap in benchmark hot
    loops where every µs counts. JSDK uses `RandomShift` (xorshift128+)
    and Rust core uses `SmallRng` for the same reason; this exposes the
    equivalent to Python so benchmark methodology stays apples-to-apples.

    Not thread-safe — construct one per worker thread / task.
    """
    def __new__(cls, seed: typing.Optional[builtins.int] = None) -> _aerospike_async_native.FastRng:
        r"""
        Create a new generator. ``seed=None`` seeds from OS entropy.
        """
    def randint(self, lo: builtins.int, hi: builtins.int) -> builtins.int:
        r"""
        Return a uniformly random integer in ``[lo, hi]`` (inclusive).
        ~5 ns per call vs CPython's ~700 ns for ``random.randint``.
        """
    def gen_below(self, max: builtins.int) -> builtins.int:
        r"""
        Return a uniformly random integer in ``[0, max)`` (exclusive).
        """
    def randrange(self, start: builtins.int, stop: builtins.int) -> builtins.int:
        r"""
        Return a uniformly random integer in ``[start, stop)`` (half-open).
        Matches CPython's ``random.Random.randrange(start, stop)`` API.
        """

class Filter:
    r"""
    Query filter definition. Currently, only one filter is allowed in a Statement, and must target a
    bin that has a secondary index (or use `*_by_index` with the index name).

    Build filters from the class static methods, for example `equal`, `range`, `contains`,
    `contains_range`, `within_region`, `within_radius`, `regions_containing_point`, and the
    corresponding `equal_by_index`, `range_by_index`, `contains_by_index`, `contains_range_by_index`,
    `within_region_by_index`, `within_radius_by_index`, and `regions_containing_point_by_index`.

    Use instance methods `context` and `expression` to attach a CDT path or expression-based index
    to a filter (for example `Filter.equal("bin", 1).context([CTX.list_index(0)])`).
    """
    def __str__(self) -> builtins.str: ...
    def __repr__(self) -> builtins.str: ...
    def context(self, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.Filter:
        r"""
        Attach a CDT context path for a secondary index on a nested list or map element.
        """
    def expression(self, exp: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.Filter:
        r"""
        Attach the expression used when the secondary index was created with
        `create_index_using_expression`.
        """
    @staticmethod
    def equal(bin_name: builtins.str, value: typing.Any) -> _aerospike_async_native.Filter: ...
    @staticmethod
    def equal_by_index(index_name: builtins.str, value: typing.Any) -> _aerospike_async_native.Filter: ...
    @staticmethod
    def range(bin_name: builtins.str, begin: typing.Any, end: typing.Any) -> _aerospike_async_native.Filter: ...
    @staticmethod
    def range_by_index(index_name: builtins.str, begin: typing.Any, end: typing.Any) -> _aerospike_async_native.Filter: ...
    @staticmethod
    def contains(bin_name: builtins.str, value: typing.Any, cit: typing.Optional[_aerospike_async_native.CollectionIndexType]) -> _aerospike_async_native.Filter: ...
    @staticmethod
    def contains_by_index(index_name: builtins.str, value: typing.Any, cit: typing.Optional[_aerospike_async_native.CollectionIndexType]) -> _aerospike_async_native.Filter: ...
    @staticmethod
    def contains_range(bin_name: builtins.str, begin: typing.Any, end: typing.Any, cit: typing.Optional[_aerospike_async_native.CollectionIndexType]) -> _aerospike_async_native.Filter: ...
    @staticmethod
    def contains_range_by_index(index_name: builtins.str, begin: typing.Any, end: typing.Any, cit: typing.Optional[_aerospike_async_native.CollectionIndexType]) -> _aerospike_async_native.Filter: ...
    @staticmethod
    def within_region(bin_name: builtins.str, region: builtins.str, cit: typing.Optional[_aerospike_async_native.CollectionIndexType]) -> _aerospike_async_native.Filter: ...
    @staticmethod
    def within_region_by_index(index_name: builtins.str, region: builtins.str, cit: typing.Optional[_aerospike_async_native.CollectionIndexType]) -> _aerospike_async_native.Filter: ...
    @staticmethod
    def within_radius(bin_name: builtins.str, lng: builtins.float, lat: builtins.float, radius: builtins.float, cit: typing.Optional[_aerospike_async_native.CollectionIndexType]) -> _aerospike_async_native.Filter:
        r"""
        Public API uses (lng, lat) to match GeoJSON [longitude, latitude].
        """
    @staticmethod
    def within_radius_by_index(index_name: builtins.str, lng: builtins.float, lat: builtins.float, radius: builtins.float, cit: typing.Optional[_aerospike_async_native.CollectionIndexType]) -> _aerospike_async_native.Filter: ...
    @staticmethod
    def regions_containing_point(bin_name: builtins.str, point: builtins.str, cit: typing.Optional[_aerospike_async_native.CollectionIndexType]) -> _aerospike_async_native.Filter: ...
    @staticmethod
    def regions_containing_point_by_index(index_name: builtins.str, point: builtins.str, cit: typing.Optional[_aerospike_async_native.CollectionIndexType]) -> _aerospike_async_native.Filter: ...

class FilterExpression:
    r"""
    Filter expression, which can be applied to most commands, to control which records are
    affected by the command.
    """
    @staticmethod
    def key(exp_type: _aerospike_async_native.ExpType) -> _aerospike_async_native.FilterExpression:
        r"""
        Create a record key expression of specified type.
        """
    @staticmethod
    def key_exists() -> _aerospike_async_native.FilterExpression:
        r"""
        Create function that returns if the primary key is stored in the record meta data
        as a boolean expression. This would occur when `send_key` is true on record write.
        """
    @staticmethod
    def int_bin(name: builtins.str) -> _aerospike_async_native.FilterExpression:
        r"""
        Create 64 bit int bin expression.
        """
    @staticmethod
    def string_bin(name: builtins.str) -> _aerospike_async_native.FilterExpression:
        r"""
        Create string bin expression.
        """
    @staticmethod
    def blob_bin(name: builtins.str) -> _aerospike_async_native.FilterExpression:
        r"""
        Create blob bin expression.
        """
    @staticmethod
    def bool_bin(name: builtins.str) -> _aerospike_async_native.FilterExpression:
        r"""
        Create boolean bin expression.
        """
    @staticmethod
    def float_bin(name: builtins.str) -> _aerospike_async_native.FilterExpression:
        r"""
        Create 64 bit float bin expression.
        """
    @staticmethod
    def geo_bin(name: builtins.str) -> _aerospike_async_native.FilterExpression:
        r"""
        Create geo bin expression.
        """
    @staticmethod
    def list_bin(name: builtins.str) -> _aerospike_async_native.FilterExpression:
        r"""
        Create list bin expression.
        """
    @staticmethod
    def map_bin(name: builtins.str) -> _aerospike_async_native.FilterExpression:
        r"""
        Create map bin expression.
        """
    @staticmethod
    def hll_bin(name: builtins.str) -> _aerospike_async_native.FilterExpression:
        r"""
        Create a HLL bin expression
        """
    @staticmethod
    def bin_exists(name: builtins.str) -> _aerospike_async_native.FilterExpression:
        r"""
        Create function that returns if bin of specified name exists.
        """
    @staticmethod
    def bin_type(name: builtins.str) -> _aerospike_async_native.FilterExpression:
        r"""
        Create function that returns bin's integer particle type.
        """
    @staticmethod
    def set_name() -> _aerospike_async_native.FilterExpression:
        r"""
        Create function that returns record set name string.
        """
    @staticmethod
    def record_size() -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns the record size. Usually evaluates quickly because
        record metadata is cached in memory. Requires server version 7.0+.
        """
    @staticmethod
    def device_size() -> _aerospike_async_native.FilterExpression:
        r"""
        Create function that returns record size on disk. If server storage-engine is
        memory, then zero is returned. Deprecated: use record_size() for server version 7.0+.
        Implemented via record_size() for server 7.0+.
        """
    @staticmethod
    def memory_size() -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns record size in memory. Deprecated: use record_size() for server 7.0+.
        Implemented via record_size() for server 7.0+.
        """
    @staticmethod
    def last_update() -> _aerospike_async_native.FilterExpression:
        r"""
        Create function that returns record last update time expressed as 64 bit integer
        nanoseconds since 1970-01-01 epoch.
        """
    @staticmethod
    def since_update() -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns milliseconds since the record was last updated.
        This expression usually evaluates quickly because record meta data is cached in memory.
        """
    @staticmethod
    def void_time() -> _aerospike_async_native.FilterExpression:
        r"""
        Create function that returns record expiration time expressed as 64 bit integer
        nanoseconds since 1970-01-01 epoch.
        """
    @staticmethod
    def ttl() -> _aerospike_async_native.FilterExpression:
        r"""
        Create function that returns record expiration time (time to live) in integer seconds.
        """
    @staticmethod
    def is_tombstone() -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns if record has been deleted and is still in tombstone state.
        This expression usually evaluates quickly because record meta data is cached in memory.
        """
    @staticmethod
    def digest_modulo(modulo: builtins.int) -> _aerospike_async_native.FilterExpression:
        r"""
        Create function that returns record digest modulo as integer.
        """
    @staticmethod
    def regex_compare(regex: builtins.str, flags: builtins.int, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create function like regular expression string operation.
        """
    @staticmethod
    def geo_compare(left: _aerospike_async_native.FilterExpression, right: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create compare geospatial operation.
        """
    @staticmethod
    def int_val(val: builtins.int) -> _aerospike_async_native.FilterExpression:
        r"""
        Creates 64 bit integer value
        """
    @staticmethod
    def bool_val(val: builtins.bool) -> _aerospike_async_native.FilterExpression:
        r"""
        Creates a Boolean value
        """
    @staticmethod
    def string_val(val: builtins.str) -> _aerospike_async_native.FilterExpression:
        r"""
        Creates String bin value
        """
    @staticmethod
    def float_val(val: builtins.float) -> _aerospike_async_native.FilterExpression:
        r"""
        Creates 64 bit float bin value
        """
    @staticmethod
    def blob_val(val: typing.Sequence[builtins.int]) -> _aerospike_async_native.FilterExpression:
        r"""
        Creates Blob bin value
        """
    @staticmethod
    def list_val(val: typing.Sequence[typing.Any]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create List bin value.
        """
    @staticmethod
    def map_val(val: typing.Any) -> _aerospike_async_native.FilterExpression:
        r"""
        Create Map bin value.
        Converts HashMap to BTreeMap (OrderedMap) for exact byte-level matching.
        map_val accepts both HashMap and BTreeMap (OrderedMap) via the MapLike trait.
        We use BTreeMap to ensure deterministic key ordering for serialization matching.
        """
    @staticmethod
    def geo_val(val: builtins.str) -> _aerospike_async_native.FilterExpression:
        r"""
        Create geospatial json string value.
        """
    @staticmethod
    def nil() -> _aerospike_async_native.FilterExpression:
        r"""
        Create a Nil PHPValue
        """
    @staticmethod
    def infinity() -> _aerospike_async_native.FilterExpression:
        r"""
        Create an infinity value for expression and CDT range boundaries.
        """
    @staticmethod
    def wildcard() -> _aerospike_async_native.FilterExpression:
        r"""
        Create a wildcard value for expression and CDT value matching.
        """
    @staticmethod
    def not_(exp: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create "not" operator expression.
        """
    @staticmethod
    def and_(exps: typing.Sequence[_aerospike_async_native.FilterExpression]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create "and" (&&) operator that applies to a variable number of expressions.
        // (a > 5 || a == 0) && b < 3
        """
    @staticmethod
    def or_(exps: typing.Sequence[_aerospike_async_native.FilterExpression]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create "or" (||) operator that applies to a variable number of expressions.
        """
    @staticmethod
    def xor(exps: typing.Sequence[_aerospike_async_native.FilterExpression]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create "xor" (^) operator that applies to a variable number of expressions.
        """
    @staticmethod
    def eq(left: _aerospike_async_native.FilterExpression, right: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create equal (==) expression.
        """
    @staticmethod
    def ne(left: _aerospike_async_native.FilterExpression, right: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create not equal (!=) expression
        """
    @staticmethod
    def gt(left: _aerospike_async_native.FilterExpression, right: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create greater than (>) operation.
        """
    @staticmethod
    def ge(left: _aerospike_async_native.FilterExpression, right: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create greater than or equal (>=) operation.
        """
    @staticmethod
    def lt(left: _aerospike_async_native.FilterExpression, right: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create less than (<) operation.
        """
    @staticmethod
    def le(left: _aerospike_async_native.FilterExpression, right: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create less than or equals (<=) operation.
        """
    @staticmethod
    def num_add(exps: typing.Sequence[_aerospike_async_native.FilterExpression]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create "add" (+) operator that applies to a variable number of expressions.
        Return sum of all `FilterExpressions` given. All arguments must resolve to the same type (integer or float).
        Requires server version 5.6.0+.
        """
    @staticmethod
    def num_sub(exps: typing.Sequence[_aerospike_async_native.FilterExpression]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create "subtract" (-) operator that applies to a variable number of expressions.
        If only one `FilterExpressions` is provided, return the negation of that argument.
        Otherwise, return the sum of the 2nd to Nth `FilterExpressions` subtracted from the 1st
        `FilterExpressions`. All `FilterExpressions` must resolve to the same type (integer or float).
        Requires server version 5.6.0+.
        """
    @staticmethod
    def num_mul(exps: typing.Sequence[_aerospike_async_native.FilterExpression]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create "multiply" (*) operator that applies to a variable number of expressions.
        Return the product of all `FilterExpressions`. If only one `FilterExpressions` is supplied, return
        that `FilterExpressions`. All `FilterExpressions` must resolve to the same type (integer or float).
        Requires server version 5.6.0+.
        """
    @staticmethod
    def num_div(exps: typing.Sequence[_aerospike_async_native.FilterExpression]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create "divide" (/) operator that applies to a variable number of expressions.
        If there is only one `FilterExpressions`, returns the reciprocal for that `FilterExpressions`.
        Otherwise, return the first `FilterExpressions` divided by the product of the rest.
        All `FilterExpressions` must resolve to the same type (integer or float).
        Requires server version 5.6.0+.
        """
    @staticmethod
    def num_pow(base: _aerospike_async_native.FilterExpression, exponent: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create "power" operator that raises a "base" to the "exponent" power.
        All arguments must resolve to floats.
        Requires server version 5.6.0+.
        """
    @staticmethod
    def num_log(num: _aerospike_async_native.FilterExpression, base: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create "log" operator for logarithm of "num" with base "base".
        All arguments must resolve to floats.
        Requires server version 5.6.0+.
        """
    @staticmethod
    def num_mod(numerator: _aerospike_async_native.FilterExpression, denominator: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create "modulo" (%) operator that determines the remainder of "numerator"
        divided by "denominator". All arguments must resolve to integers.
        Requires server version 5.6.0+.
        """
    @staticmethod
    def num_abs(value: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create operator that returns absolute value of a number.
        All arguments must resolve to integer or float.
        Requires server version 5.6.0+.
        """
    @staticmethod
    def num_floor(num: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that rounds a floating point number down to the closest integer value.
        The return type is float.
        """
    @staticmethod
    def num_ceil(num: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that rounds a floating point number up to the closest integer value.
        The return type is float.
        Requires server version 5.6.0+.
        """
    @staticmethod
    def to_int(num: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that converts an integer to a float.
        Requires server version 5.6.0+.
        """
    @staticmethod
    def to_float(num: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that converts a float to an integer.
        Requires server version 5.6.0+.
        """
    @staticmethod
    def int_and(exps: typing.Sequence[_aerospike_async_native.FilterExpression]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create integer "and" (&) operator that is applied to two or more integers.
        All arguments must resolve to integers.
        Requires server version 5.6.0+.
        """
    @staticmethod
    def int_or(exps: typing.Sequence[_aerospike_async_native.FilterExpression]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create integer "or" (|) operator that is applied to two or more integers.
        All arguments must resolve to integers.
        Requires server version 5.6.0+.
        """
    @staticmethod
    def int_xor(exps: typing.Sequence[_aerospike_async_native.FilterExpression]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create integer "xor" (^) operator that is applied to two or more integers.
        All arguments must resolve to integers.
        Requires server version 5.6.0+.
        """
    @staticmethod
    def int_not(exp: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create integer "not" (~) operator.
        Requires server version 5.6.0+.
        """
    @staticmethod
    def int_lshift(value: _aerospike_async_native.FilterExpression, shift: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create integer "left shift" (<<) operator.
        Requires server version 5.6.0+.
        """
    @staticmethod
    def int_rshift(value: _aerospike_async_native.FilterExpression, shift: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create integer "logical right shift" (>>>) operator.
        Requires server version 5.6.0+.
        """
    @staticmethod
    def int_arshift(value: _aerospike_async_native.FilterExpression, shift: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create integer "arithmetic right shift" (>>) operator.
        The sign bit is preserved and not shifted.
        Requires server version 5.6.0+.
        """
    @staticmethod
    def int_count(exp: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns count of integer bits that are set to 1.
        Requires server version 5.6.0+
        """
    @staticmethod
    def int_lscan(value: _aerospike_async_native.FilterExpression, search: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that scans integer bits from left (most significant bit) to
        right (least significant bit), looking for a search bit value. When the
        search value is found, the index of that bit (where the most significant bit is
        index 0) is returned. If "search" is true, the scan will search for the bit
        value 1. If "search" is false it will search for bit value 0.
        Requires server version 5.6.0+.
        """
    @staticmethod
    def int_rscan(value: _aerospike_async_native.FilterExpression, search: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that scans integer bits from right (least significant bit) to
        left (most significant bit), looking for a search bit value. When the
        search value is found, the index of that bit (where the most significant bit is
        index 0) is returned. If "search" is true, the scan will search for the bit
        value 1. If "search" is false it will search for bit value 0.
        Requires server version 5.6.0+.
        """
    @staticmethod
    def min(exps: typing.Sequence[_aerospike_async_native.FilterExpression]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns the minimum value in a variable number of expressions.
        All arguments must be the same type (integer or float).
        Requires server version 5.6.0+.
        """
    @staticmethod
    def max(exps: typing.Sequence[_aerospike_async_native.FilterExpression]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns the maximum value in a variable number of expressions.
        All arguments must be the same type (integer or float).
        Requires server version 5.6.0+.
        """
    @staticmethod
    def cond(exps: typing.Sequence[_aerospike_async_native.FilterExpression]) -> _aerospike_async_native.FilterExpression:
        r"""
        Conditionally select an expression from a variable number of expression pairs
        followed by default expression action.
        Requires server version 5.6.0+.
        ```
        // Args Format: bool exp1, action exp1, bool exp2, action exp2, ..., action-default
        // Apply operator based on type.
        """
    @staticmethod
    def exp_let(exps: typing.Sequence[_aerospike_async_native.FilterExpression]) -> _aerospike_async_native.FilterExpression:
        r"""
        Define variables and expressions in scope.
        Requires server version 5.6.0+.
        ```
        // 5 < a < 10
        """
    @staticmethod
    def def_(name: builtins.str, value: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Assign variable to an expression that can be accessed later.
        Requires server version 5.6.0+.
        ```
        // 5 < a < 10
        """
    @staticmethod
    def var(name: builtins.str) -> _aerospike_async_native.FilterExpression:
        r"""
        Retrieve expression value from a variable.
        Requires server version 5.6.0+.
        """
    def __richcmp__(self, other: _aerospike_async_native.FilterExpression, op: int) -> builtins.bool: ...
    def __hash__(self) -> builtins.int: ...
    def _debug_inner(self) -> builtins.str:
        r"""
        Return the debug representation of the inner expression (used for equality).
        Exposed for inspection; same string used by __eq__.
        """
    def base64(self) -> builtins.str:
        r"""
        Encode the expression to a base64 string.
        """
    @staticmethod
    def from_base64(b64: builtins.str) -> _aerospike_async_native.FilterExpression:
        r"""
        Create an expression from a base64-encoded expression string.
        """
    @staticmethod
    def unknown() -> _aerospike_async_native.FilterExpression:
        r"""
        Create unknown value. Used to intentionally fail an expression.
        The failure can be ignored with `ExpWriteFlags` `EVAL_NO_FAIL`
        or `ExpReadFlags` `EVAL_NO_FAIL`.
        Requires server version 5.6.0+.
        """
    @staticmethod
    def list_size(bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns list size.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def list_get_by_index(return_type: _aerospike_async_native.ListReturnType, value_type: _aerospike_async_native.ExpType, index: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects list item identified by index and returns
        selected data specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def list_get_by_rank(return_type: _aerospike_async_native.ListReturnType, value_type: _aerospike_async_native.ExpType, rank: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects list item identified by rank and returns
        selected data specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def list_get_by_value(return_type: _aerospike_async_native.ListReturnType, value: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects list items identified by value and returns selected data
        specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def list_get_by_value_range(return_type: _aerospike_async_native.ListReturnType, value_begin: typing.Optional[_aerospike_async_native.FilterExpression], value_end: typing.Optional[_aerospike_async_native.FilterExpression], bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects list items identified by value range (valueBegin inclusive, valueEnd exclusive)
        and returns selected data specified by returnType.
        If valueBegin is None, the range is less than valueEnd. If valueEnd is None, the range is greater than equal to valueBegin.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def list_get_by_value_list(return_type: _aerospike_async_native.ListReturnType, values: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects list items identified by values and returns selected data
        specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def list_get_by_index_range(return_type: _aerospike_async_native.ListReturnType, index: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects list items starting at specified index to the end of list
        and returns selected data specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def list_get_by_index_range_count(return_type: _aerospike_async_native.ListReturnType, index: _aerospike_async_native.FilterExpression, count: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects "count" list items starting at specified index
        and returns selected data specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def list_get_by_rank_range(return_type: _aerospike_async_native.ListReturnType, rank: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects list items starting at specified rank to the last ranked item
        and returns selected data specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def list_get_by_rank_range_count(return_type: _aerospike_async_native.ListReturnType, rank: _aerospike_async_native.FilterExpression, count: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects "count" list items starting at specified rank and returns
        selected data specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def list_get_by_value_relative_rank_range(return_type: _aerospike_async_native.ListReturnType, value: _aerospike_async_native.FilterExpression, rank: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects list items nearest to value and greater by relative rank
        and returns selected data specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def list_get_by_value_relative_rank_range_count(return_type: _aerospike_async_native.ListReturnType, value: _aerospike_async_native.FilterExpression, rank: _aerospike_async_native.FilterExpression, count: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects list items nearest to value and greater by relative rank with a count limit
        and returns selected data specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def list_append(policy: _aerospike_async_native.ListPolicy, value: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that appends value to end of list.
        """
    @staticmethod
    def list_append_items(policy: _aerospike_async_native.ListPolicy, list: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that appends list items to end of list.
        """
    @staticmethod
    def list_insert(policy: _aerospike_async_native.ListPolicy, index: _aerospike_async_native.FilterExpression, value: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that inserts value to specified index of list.
        """
    @staticmethod
    def list_insert_items(policy: _aerospike_async_native.ListPolicy, index: _aerospike_async_native.FilterExpression, list: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that inserts each input list item starting at specified index of list.
        """
    @staticmethod
    def list_increment(policy: _aerospike_async_native.ListPolicy, index: _aerospike_async_native.FilterExpression, value: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that increments list[index] by value.
        """
    @staticmethod
    def list_set(policy: _aerospike_async_native.ListPolicy, index: _aerospike_async_native.FilterExpression, value: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that sets item value at specified index in list.
        """
    @staticmethod
    def list_clear(bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes all items in list.
        """
    @staticmethod
    def list_sort(sort_flags: _aerospike_async_native.ListSortFlags, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that sorts list according to sort_flags.
        """
    @staticmethod
    def list_remove_by_value(return_type: _aerospike_async_native.ListReturnType, value: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes list items identified by value.
        """
    @staticmethod
    def list_remove_by_value_list(return_type: _aerospike_async_native.ListReturnType, values: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes list items identified by values.
        """
    @staticmethod
    def list_remove_by_value_range(return_type: _aerospike_async_native.ListReturnType, value_begin: typing.Optional[_aerospike_async_native.FilterExpression], value_end: typing.Optional[_aerospike_async_native.FilterExpression], bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes list items identified by value range
        (value_begin inclusive, value_end exclusive).
        """
    @staticmethod
    def list_remove_by_value_relative_rank_range(return_type: _aerospike_async_native.ListReturnType, value: _aerospike_async_native.FilterExpression, rank: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes list items nearest to value and greater by relative rank.
        """
    @staticmethod
    def list_remove_by_value_relative_rank_range_count(return_type: _aerospike_async_native.ListReturnType, value: _aerospike_async_native.FilterExpression, rank: _aerospike_async_native.FilterExpression, count: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes list items nearest to value and greater by relative rank
        with a count limit.
        """
    @staticmethod
    def list_remove_by_index(return_type: _aerospike_async_native.ListReturnType, index: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes list item identified by index.
        """
    @staticmethod
    def list_remove_by_index_range(return_type: _aerospike_async_native.ListReturnType, index: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes list items starting at specified index to the end of list.
        """
    @staticmethod
    def list_remove_by_index_range_count(return_type: _aerospike_async_native.ListReturnType, index: _aerospike_async_native.FilterExpression, count: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes "count" list items starting at specified index.
        """
    @staticmethod
    def list_remove_by_rank(return_type: _aerospike_async_native.ListReturnType, rank: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes list item identified by rank.
        """
    @staticmethod
    def list_remove_by_rank_range(return_type: _aerospike_async_native.ListReturnType, rank: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes list items starting at specified rank to the last ranked item.
        """
    @staticmethod
    def list_remove_by_rank_range_count(return_type: _aerospike_async_native.ListReturnType, rank: _aerospike_async_native.FilterExpression, count: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes "count" list items starting at specified rank.
        """
    @staticmethod
    def map_size(bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns map size.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def map_get_by_key(return_type: _aerospike_async_native.MapReturnType, value_type: _aerospike_async_native.ExpType, key: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects map item identified by key and returns selected data
        specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def map_get_by_rank(return_type: _aerospike_async_native.MapReturnType, value_type: _aerospike_async_native.ExpType, rank: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects map item identified by rank and returns selected data
        specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def map_get_by_index(return_type: _aerospike_async_native.MapReturnType, value_type: _aerospike_async_native.ExpType, index: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects map item identified by index and returns selected data
        specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def map_get_by_value(return_type: _aerospike_async_native.MapReturnType, value: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects map items identified by value and returns selected data
        specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def map_get_by_value_range(return_type: _aerospike_async_native.MapReturnType, value_begin: typing.Optional[_aerospike_async_native.FilterExpression], value_end: typing.Optional[_aerospike_async_native.FilterExpression], bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects map items identified by value range (valueBegin inclusive, valueEnd exclusive)
        and returns selected data specified by returnType.
        If valueBegin is None, the range is less than valueEnd. If valueEnd is None, the range is greater than equal to valueBegin.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def map_get_by_value_list(return_type: _aerospike_async_native.MapReturnType, values: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects map items identified by values and returns selected data
        specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def map_get_by_key_range(return_type: _aerospike_async_native.MapReturnType, key_begin: typing.Optional[_aerospike_async_native.FilterExpression], key_end: typing.Optional[_aerospike_async_native.FilterExpression], bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects map items identified by key range (keyBegin inclusive, keyEnd exclusive)
        and returns selected data specified by returnType.
        If keyBegin is None, the range is less than keyEnd. If keyEnd is None, the range is greater than equal to keyBegin.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def map_get_by_key_list(return_type: _aerospike_async_native.MapReturnType, keys: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects map items identified by keys and returns selected data
        specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def map_get_by_key_relative_index_range(return_type: _aerospike_async_native.MapReturnType, key: _aerospike_async_native.FilterExpression, index: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects map items nearest to key and greater by index
        and returns selected data specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def map_get_by_key_relative_index_range_count(return_type: _aerospike_async_native.MapReturnType, key: _aerospike_async_native.FilterExpression, index: _aerospike_async_native.FilterExpression, count: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects map items nearest to key and greater by index with a count limit
        and returns selected data specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def map_get_by_value_relative_rank_range(return_type: _aerospike_async_native.MapReturnType, value: _aerospike_async_native.FilterExpression, rank: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects map items nearest to value and greater by relative rank
        and returns selected data specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def map_get_by_value_relative_rank_range_count(return_type: _aerospike_async_native.MapReturnType, value: _aerospike_async_native.FilterExpression, rank: _aerospike_async_native.FilterExpression, count: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects map items nearest to value and greater by relative rank with a count limit
        and returns selected data specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def map_get_by_index_range(return_type: _aerospike_async_native.MapReturnType, index: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects map items starting at specified index to the end of map
        and returns selected data specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def map_get_by_index_range_count(return_type: _aerospike_async_native.MapReturnType, index: _aerospike_async_native.FilterExpression, count: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects "count" map items starting at specified index
        and returns selected data specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def map_get_by_rank_range(return_type: _aerospike_async_native.MapReturnType, rank: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects map items starting at specified rank to the last ranked item
        and returns selected data specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def map_get_by_rank_range_count(return_type: _aerospike_async_native.MapReturnType, rank: _aerospike_async_native.FilterExpression, count: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that selects "count" map items starting at specified rank
        and returns selected data specified by returnType.
        Supports nested CDT operations via optional CTX contexts.
        """
    @staticmethod
    def map_put(policy: _aerospike_async_native.MapPolicy, key: _aerospike_async_native.FilterExpression, value: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that writes key/value item to map bin.
        """
    @staticmethod
    def map_put_items(policy: _aerospike_async_native.MapPolicy, map: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that writes each map item to map bin.
        """
    @staticmethod
    def map_increment(policy: _aerospike_async_native.MapPolicy, key: _aerospike_async_native.FilterExpression, incr: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that increments values by incr for all items identified by key.
        """
    @staticmethod
    def map_clear(bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes all items in map.
        """
    @staticmethod
    def map_remove_by_key(return_type: _aerospike_async_native.MapReturnType, key: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes map item identified by key.
        """
    @staticmethod
    def map_remove_by_key_list(return_type: _aerospike_async_native.MapReturnType, keys: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes map items identified by keys.
        """
    @staticmethod
    def map_remove_by_key_range(return_type: _aerospike_async_native.MapReturnType, key_begin: typing.Optional[_aerospike_async_native.FilterExpression], key_end: typing.Optional[_aerospike_async_native.FilterExpression], bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes map items identified by key range
        (key_begin inclusive, key_end exclusive).
        """
    @staticmethod
    def map_remove_by_key_relative_index_range(return_type: _aerospike_async_native.MapReturnType, key: _aerospike_async_native.FilterExpression, index: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes map items nearest to key and greater by index.
        """
    @staticmethod
    def map_remove_by_key_relative_index_range_count(return_type: _aerospike_async_native.MapReturnType, key: _aerospike_async_native.FilterExpression, index: _aerospike_async_native.FilterExpression, count: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes map items nearest to key and greater by index
        with a count limit.
        """
    @staticmethod
    def map_remove_by_value(return_type: _aerospike_async_native.MapReturnType, value: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes map items identified by value.
        """
    @staticmethod
    def map_remove_by_value_list(return_type: _aerospike_async_native.MapReturnType, values: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes map items identified by values.
        """
    @staticmethod
    def map_remove_by_value_range(return_type: _aerospike_async_native.MapReturnType, value_begin: typing.Optional[_aerospike_async_native.FilterExpression], value_end: typing.Optional[_aerospike_async_native.FilterExpression], bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes map items identified by value range
        (value_begin inclusive, value_end exclusive).
        """
    @staticmethod
    def map_remove_by_value_relative_rank_range(return_type: _aerospike_async_native.MapReturnType, value: _aerospike_async_native.FilterExpression, rank: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes map items nearest to value and greater by relative rank.
        """
    @staticmethod
    def map_remove_by_value_relative_rank_range_count(return_type: _aerospike_async_native.MapReturnType, value: _aerospike_async_native.FilterExpression, rank: _aerospike_async_native.FilterExpression, count: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes map items nearest to value and greater by relative rank
        with a count limit.
        """
    @staticmethod
    def map_remove_by_index(return_type: _aerospike_async_native.MapReturnType, index: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes map item identified by index.
        """
    @staticmethod
    def map_remove_by_index_range(return_type: _aerospike_async_native.MapReturnType, index: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes map items starting at specified index to the end of map.
        """
    @staticmethod
    def map_remove_by_index_range_count(return_type: _aerospike_async_native.MapReturnType, index: _aerospike_async_native.FilterExpression, count: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes "count" map items starting at specified index.
        """
    @staticmethod
    def map_remove_by_rank(return_type: _aerospike_async_native.MapReturnType, rank: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes map item identified by rank.
        """
    @staticmethod
    def map_remove_by_rank_range(return_type: _aerospike_async_native.MapReturnType, rank: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes map items starting at specified rank to the last ranked item.
        """
    @staticmethod
    def map_remove_by_rank_range_count(return_type: _aerospike_async_native.MapReturnType, rank: _aerospike_async_native.FilterExpression, count: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes "count" map items starting at specified rank.
        """
    @staticmethod
    def bit_resize(policy: _aerospike_async_native.BitPolicy, byte_size: _aerospike_async_native.FilterExpression, resize_flags: _aerospike_async_native.BitwiseResizeFlags, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that resizes byte[] to byte_size according to resize_flags
        and returns byte[].
        """
    @staticmethod
    def bit_insert(policy: _aerospike_async_native.BitPolicy, byte_offset: _aerospike_async_native.FilterExpression, value: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that inserts value bytes into byte[] bin at byte_offset
        and returns byte[].
        """
    @staticmethod
    def bit_remove(policy: _aerospike_async_native.BitPolicy, byte_offset: _aerospike_async_native.FilterExpression, byte_size: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that removes bytes from byte[] bin at byte_offset for byte_size
        and returns byte[].
        """
    @staticmethod
    def bit_set(policy: _aerospike_async_native.BitPolicy, bit_offset: _aerospike_async_native.FilterExpression, bit_size: _aerospike_async_native.FilterExpression, value: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that sets value on byte[] bin at bit_offset for bit_size
        and returns byte[].
        """
    @staticmethod
    def bit_or(policy: _aerospike_async_native.BitPolicy, bit_offset: _aerospike_async_native.FilterExpression, bit_size: _aerospike_async_native.FilterExpression, value: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that performs bitwise "or" on value and byte[] bin at bit_offset
        for bit_size and returns byte[].
        """
    @staticmethod
    def bit_xor(policy: _aerospike_async_native.BitPolicy, bit_offset: _aerospike_async_native.FilterExpression, bit_size: _aerospike_async_native.FilterExpression, value: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that performs bitwise "xor" on value and byte[] bin at bit_offset
        for bit_size and returns byte[].
        """
    @staticmethod
    def bit_and(policy: _aerospike_async_native.BitPolicy, bit_offset: _aerospike_async_native.FilterExpression, bit_size: _aerospike_async_native.FilterExpression, value: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that performs bitwise "and" on value and byte[] bin at bit_offset
        for bit_size and returns byte[].
        """
    @staticmethod
    def bit_not(policy: _aerospike_async_native.BitPolicy, bit_offset: _aerospike_async_native.FilterExpression, bit_size: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that negates byte[] bin starting at bit_offset for bit_size
        and returns byte[].
        """
    @staticmethod
    def bit_lshift(policy: _aerospike_async_native.BitPolicy, bit_offset: _aerospike_async_native.FilterExpression, bit_size: _aerospike_async_native.FilterExpression, shift: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that shifts left byte[] bin starting at bit_offset for bit_size
        and returns byte[].
        """
    @staticmethod
    def bit_rshift(policy: _aerospike_async_native.BitPolicy, bit_offset: _aerospike_async_native.FilterExpression, bit_size: _aerospike_async_native.FilterExpression, shift: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that shifts right byte[] bin starting at bit_offset for bit_size
        and returns byte[].
        """
    @staticmethod
    def bit_add(policy: _aerospike_async_native.BitPolicy, bit_offset: _aerospike_async_native.FilterExpression, bit_size: _aerospike_async_native.FilterExpression, value: _aerospike_async_native.FilterExpression, signed: builtins.bool, action: _aerospike_async_native.BitwiseOverflowActions, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that adds value to byte[] bin starting at bit_offset for bit_size
        and returns byte[]. BitSize must be <= 64.
        """
    @staticmethod
    def bit_subtract(policy: _aerospike_async_native.BitPolicy, bit_offset: _aerospike_async_native.FilterExpression, bit_size: _aerospike_async_native.FilterExpression, value: _aerospike_async_native.FilterExpression, signed: builtins.bool, action: _aerospike_async_native.BitwiseOverflowActions, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that subtracts value from byte[] bin starting at bit_offset for bit_size
        and returns byte[]. BitSize must be <= 64.
        """
    @staticmethod
    def bit_set_int(policy: _aerospike_async_native.BitPolicy, bit_offset: _aerospike_async_native.FilterExpression, bit_size: _aerospike_async_native.FilterExpression, value: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that sets value to byte[] bin starting at bit_offset for bit_size
        and returns byte[]. BitSize must be <= 64.
        """
    @staticmethod
    def bit_get(bit_offset: _aerospike_async_native.FilterExpression, bit_size: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns bits from byte[] bin starting at bit_offset for bit_size.
        """
    @staticmethod
    def bit_count(bit_offset: _aerospike_async_native.FilterExpression, bit_size: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns integer count of set bits from byte[] bin starting at
        bit_offset for bit_size.
        """
    @staticmethod
    def bit_lscan(bit_offset: _aerospike_async_native.FilterExpression, bit_size: _aerospike_async_native.FilterExpression, value: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns integer bit offset of the first specified value bit
        in byte[] bin starting at bit_offset for bit_size.
        """
    @staticmethod
    def bit_rscan(bit_offset: _aerospike_async_native.FilterExpression, bit_size: _aerospike_async_native.FilterExpression, value: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns integer bit offset of the last specified value bit
        in byte[] bin starting at bit_offset for bit_size.
        """
    @staticmethod
    def bit_get_int(bit_offset: _aerospike_async_native.FilterExpression, bit_size: _aerospike_async_native.FilterExpression, signed: builtins.bool, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns integer from byte[] bin starting at bit_offset for bit_size.
        Signed indicates if bits should be treated as a signed number.
        """
    @staticmethod
    def hll_init(policy: _aerospike_async_native.HLLPolicy, index_bit_count: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that creates a new HLL or resets an existing HLL.
        """
    @staticmethod
    def hll_init_with_min_hash(policy: _aerospike_async_native.HLLPolicy, index_bit_count: _aerospike_async_native.FilterExpression, min_hash_count: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that creates a new HLL or resets an existing HLL with minhash bits.
        """
    @staticmethod
    def hll_add(policy: _aerospike_async_native.HLLPolicy, list: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that adds list values to a HLL set and returns HLL set.
        """
    @staticmethod
    def hll_add_with_index(policy: _aerospike_async_native.HLLPolicy, list: _aerospike_async_native.FilterExpression, index_bit_count: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that adds values to a HLL set and returns HLL set.
        If HLL bin does not exist, use index_bit_count to create HLL bin.
        """
    @staticmethod
    def hll_add_with_index_and_min_hash(policy: _aerospike_async_native.HLLPolicy, list: _aerospike_async_native.FilterExpression, index_bit_count: _aerospike_async_native.FilterExpression, min_hash_count: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that adds values to a HLL set and returns HLL set.
        If HLL bin does not exist, use index_bit_count and min_hash_count to create HLL set.
        """
    @staticmethod
    def hll_get_count(bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns estimated number of elements in the HLL bin.
        """
    @staticmethod
    def hll_get_union(list: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns a HLL object that is the union of all specified
        HLL objects in the list with the HLL bin.
        """
    @staticmethod
    def hll_get_union_count(list: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns estimated number of elements that would be contained
        by the union of these HLL objects.
        """
    @staticmethod
    def hll_get_intersect_count(list: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns estimated number of elements that would be contained
        by the intersection of these HLL objects.
        """
    @staticmethod
    def hll_get_similarity(list: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns estimated similarity of these HLL objects
        as a 64 bit float.
        """
    @staticmethod
    def hll_describe(bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns index_bit_count and min_hash_bit_count used to create
        HLL bin in a list of longs.
        """
    @staticmethod
    def hll_may_contain(list: _aerospike_async_native.FilterExpression, bin: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        Create expression that returns one if HLL bin may contain all items in the list.
        """
    @staticmethod
    def bool_loop_var(part: _aerospike_async_native.LoopVarPart) -> _aerospike_async_native.FilterExpression:
        r"""
        Create a boolean loop variable expression for use in path expressions.

        Args:
            part: Which element of the loop variable to access (``LoopVarPart``).

        Returns:
            A ``FilterExpression`` representing the boolean loop variable.
        """
    @staticmethod
    def int_loop_var(part: _aerospike_async_native.LoopVarPart) -> _aerospike_async_native.FilterExpression:
        r"""
        Create an integer loop variable expression for use in path expressions.

        Args:
            part: Which element of the loop variable to access (``LoopVarPart``).

        Returns:
            A ``FilterExpression`` representing the integer loop variable.
        """
    @staticmethod
    def float_loop_var(part: _aerospike_async_native.LoopVarPart) -> _aerospike_async_native.FilterExpression:
        r"""
        Create a float loop variable expression for use in path expressions.

        Args:
            part: Which element of the loop variable to access (``LoopVarPart``).

        Returns:
            A ``FilterExpression`` representing the float loop variable.
        """
    @staticmethod
    def string_loop_var(part: _aerospike_async_native.LoopVarPart) -> _aerospike_async_native.FilterExpression:
        r"""
        Create a string loop variable expression for use in path expressions.

        Args:
            part: Which element of the loop variable to access (``LoopVarPart``).

        Returns:
            A ``FilterExpression`` representing the string loop variable.
        """
    @staticmethod
    def list_loop_var(part: _aerospike_async_native.LoopVarPart) -> _aerospike_async_native.FilterExpression:
        r"""
        Create a list loop variable expression for use in path expressions.

        Args:
            part: Which element of the loop variable to access (``LoopVarPart``).

        Returns:
            A ``FilterExpression`` representing the list loop variable.
        """
    @staticmethod
    def map_loop_var(part: _aerospike_async_native.LoopVarPart) -> _aerospike_async_native.FilterExpression:
        r"""
        Create a map loop variable expression for use in path expressions.

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def blob_loop_var(part: _aerospike_async_native.LoopVarPart) -> _aerospike_async_native.FilterExpression:
        r"""
        Retrieve the blob part of the current loop variable.

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def hll_loop_var(part: _aerospike_async_native.LoopVarPart) -> _aerospike_async_native.FilterExpression:
        r"""
        Retrieve the HLL part of the current loop variable.

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def nil_loop_var(part: _aerospike_async_native.LoopVarPart) -> _aerospike_async_native.FilterExpression:
        r"""
        Retrieve the nil part of the current loop variable.

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def geo_json_loop_var(part: _aerospike_async_native.LoopVarPart) -> _aerospike_async_native.FilterExpression:
        r"""
        Retrieve the GeoJSON part of the current loop variable.

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def remove_result() -> _aerospike_async_native.FilterExpression:
        r"""
        Signal that the current loop element should be removed from the result.

        Used as the modify expression in :meth:`CdtOperation.modify_by_path` to
        delete matched elements rather than replace them.

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def in_list(value: _aerospike_async_native.FilterExpression, list: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        ``value`` exists in ``list``. Native ExpOp on server 8.1.2+ —
        returns a boolean expression equivalent to ``value IN list``.

        Requires Aerospike Server version >= 8.1.2.
        """
    @staticmethod
    def map_keys(map: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        All keys of a map expression as a list expression. Native ExpOp on
        server 8.1.2+ (cheaper than ``map_get_by_index_range(KEY, 0, ...)``).

        Requires Aerospike Server version >= 8.1.2.
        """
    @staticmethod
    def map_values(map: _aerospike_async_native.FilterExpression) -> _aerospike_async_native.FilterExpression:
        r"""
        All values of a map expression as a list expression. Native ExpOp
        on server 8.1.2+ (cheaper than ``map_get_by_index_range(VALUE, 0, ...)``).

        Requires Aerospike Server version >= 8.1.2.
        """
    @staticmethod
    def exp_select_by_path(return_type: _aerospike_async_native.ExpType, flag: builtins.int, bin_exp: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Select from a CDT bin expression using a path context. The
        ``flag`` is a bitwise combination of ``SelectFlags`` constants.

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def exp_modify_by_path(return_type: _aerospike_async_native.ExpType, flag: builtins.int, bin_exp: _aerospike_async_native.FilterExpression, modify_exp: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Modify a CDT bin expression using a path context. The ``flag`` is
        a bitwise combination of ``ModifyFlags`` constants.

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def exp_select_values(return_type: _aerospike_async_native.ExpType, bin_exp: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Convenience wrapper: select the *values* at every path-resolved
        location (``SelectFlags.VALUE``).

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def exp_select_map_keys(return_type: _aerospike_async_native.ExpType, bin_exp: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Convenience wrapper: select map *keys* at every path-resolved
        location (``SelectFlags.MAP_KEY``).

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def exp_select_map_entries(return_type: _aerospike_async_native.ExpType, bin_exp: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Convenience wrapper: select map *key/value pairs*
        (``SelectFlags.MAP_KEY_VALUE``).

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def exp_select_matching_tree(return_type: _aerospike_async_native.ExpType, bin_exp: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Convenience wrapper: select the *original tree shape* preserving
        only matching nodes (``SelectFlags.MATCHING_TREE``).

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def exp_modify(return_type: _aerospike_async_native.ExpType, bin_exp: _aerospike_async_native.FilterExpression, modify_exp: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Convenience wrapper: modify with default flags, failing on type
        mismatches (``ModifyFlags.DEFAULT``).

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def exp_modify_no_fail(return_type: _aerospike_async_native.ExpType, bin_exp: _aerospike_async_native.FilterExpression, modify_exp: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Convenience wrapper: modify with ``ModifyFlags.NO_FAIL`` so
        type-mismatched leaves are silently skipped instead of aborting
        the whole expression.

        Requires Aerospike Server version >= 8.1.1.
        """
    @staticmethod
    def exp_remove(return_type: _aerospike_async_native.ExpType, bin_exp: _aerospike_async_native.FilterExpression, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.FilterExpression:
        r"""
        Convenience wrapper: remove the leaves resolved by a path.
        Equivalent to ``exp_modify_by_path(t, ModifyFlags.DEFAULT, bin, FilterExpression.remove_result(), ctx)``.

        Requires Aerospike Server version >= 8.1.1.
        """

class GeoJSON:
    @property
    def value(self) -> builtins.str: ...
    @value.setter
    def value(self, value: builtins.str) -> None: ...
    def __new__(cls, v: typing.Any) -> _aerospike_async_native.GeoJSON: ...
    def as_string(self) -> builtins.str:
        r"""
        Returns a string representation of the value.
        """
    def __richcmp__(self, other: typing.Any, op: int) -> builtins.bool: ...
    def __str__(self) -> builtins.str: ...
    def __repr__(self) -> builtins.str: ...

class HLL:
    @property
    def value(self) -> builtins.list[builtins.int]: ...
    @value.setter
    def value(self, value: typing.Sequence[builtins.int]) -> None: ...
    def __new__(cls, v: typing.Sequence[builtins.int]) -> _aerospike_async_native.HLL: ...
    def as_string(self) -> builtins.str:
        r"""
        Returns a string representation of the value.
        """
    def __richcmp__(self, other: typing.Any, op: int) -> builtins.bool: ...

class HLLPolicy:
    r"""
    HLL policy for HLL operations and expressions.
    """
    @property
    def write_flags(self) -> builtins.int:
        r"""
        Get the write flags as an int bitmask.
        """
    @write_flags.setter
    def write_flags(self, value: typing.Union[ListWriteFlags, int]) -> None:
        r"""
        Set the write flags.
        flags may be HLLWriteFlags or int (bitmask).
        """
    def __new__(cls, write_flags: typing.Optional[typing.Any] = None) -> _aerospike_async_native.HLLPolicy:
        r"""
        Create a new HLLPolicy with the specified write flags.
        write_flags may be HLLWriteFlags or int (bitmask), e.g. CREATE_ONLY | NO_FAIL.
        Default is default write flags.
        """

class HllOperation:
    r"""
    HLL (HyperLogLog) operations. Create HLL operations used by the client's `operate()` method.
    """
    @staticmethod
    def init(bin_name: builtins.str, index_bit_count: builtins.int, min_hash_bit_count: builtins.int = -1, flags: typing.Optional[typing.Any] = None) -> _aerospike_async_native.HllOperation:
        r"""
        Create HLL init operation.
        Server creates a new HLL or resets an existing HLL.
        Server does not return a value.

        ``flags`` may be an ``HLLWriteFlags`` member, a combined bitmask
        (``HLLWriteFlags.X | HLLWriteFlags.Y``), or a raw ``int``.
        """
    @staticmethod
    def add(bin_name: builtins.str, values: typing.Sequence[typing.Any], index_bit_count: builtins.int = -1, min_hash_bit_count: builtins.int = -1, flags: typing.Optional[typing.Any] = None) -> _aerospike_async_native.HllOperation:
        r"""
        Create HLL add operation.
        Server adds values to HLL set. If HLL bin does not exist and index_bit_count is set,
        a new HLL bin will be created.
        Server returns number of entries that caused HLL to update a register.

        ``flags`` may be an ``HLLWriteFlags`` member, a combined bitmask
        (``HLLWriteFlags.X | HLLWriteFlags.Y``), or a raw ``int``.
        """
    @staticmethod
    def get_count(bin_name: builtins.str) -> _aerospike_async_native.HllOperation:
        r"""
        Create HLL get_count operation.
        Server returns estimated number of elements in the HLL bin.
        """
    @staticmethod
    def describe(bin_name: builtins.str) -> _aerospike_async_native.HllOperation:
        r"""
        Create HLL describe operation.
        Server returns index_bit_count and min_hash_bit_count used to create HLL bin
        in a list of longs. The list size is 2.
        """
    @staticmethod
    def refresh_count(bin_name: builtins.str) -> _aerospike_async_native.HllOperation:
        r"""
        Create HLL refresh_count operation.
        Server updates the cached count (if stale) and returns the count.
        """
    @staticmethod
    def fold(bin_name: builtins.str, index_bit_count: builtins.int) -> _aerospike_async_native.HllOperation:
        r"""
        Create HLL fold operation.
        Server folds index_bit_count to the specified value.
        This can only be applied when min_hash_bit_count on the HLL bin is 0.
        Server does not return a value.
        """
    @staticmethod
    def get_union(bin_name: builtins.str, hll_list: typing.Sequence[typing.Any]) -> _aerospike_async_native.HllOperation:
        r"""
        Create HLL get_union operation.
        Server returns an HLL object that is the union of all specified HLL objects
        in the list with the HLL bin.
        """
    @staticmethod
    def get_union_count(bin_name: builtins.str, hll_list: typing.Sequence[typing.Any]) -> _aerospike_async_native.HllOperation:
        r"""
        Create HLL get_union_count operation.
        Server returns estimated number of elements that would be contained
        by the union of these HLL objects.
        """
    @staticmethod
    def get_intersect_count(bin_name: builtins.str, hll_list: typing.Sequence[typing.Any]) -> _aerospike_async_native.HllOperation:
        r"""
        Create HLL get_intersect_count operation.
        Server returns estimated number of elements that would be contained
        by the intersection of these HLL objects.
        """
    @staticmethod
    def get_similarity(bin_name: builtins.str, hll_list: typing.Sequence[typing.Any]) -> _aerospike_async_native.HllOperation:
        r"""
        Create HLL get_similarity operation.
        Server returns estimated similarity of these HLL objects. Return type is a double.
        """
    @staticmethod
    def set_union(bin_name: builtins.str, hll_list: typing.Sequence[typing.Any], flags: typing.Optional[typing.Any] = None) -> _aerospike_async_native.HllOperation:
        r"""
        Create HLL set_union operation.
        Server sets union of specified HLL objects with HLL bin.
        Server does not return a value.

        ``flags`` may be an ``HLLWriteFlags`` member, a combined bitmask
        (``HLLWriteFlags.X | HLLWriteFlags.Y``), or a raw ``int``.
        """

class IndexTask:
    def query_status(self) -> typing.Awaitable[TaskStatus]: ...
    def wait_till_complete(self, sleep_time: builtins.float = 0.25, max_attempts: builtins.int = 80) -> typing.Awaitable[bool]: ...
    def query_status_blocking(self) -> _aerospike_async_native.TaskStatus:
        r"""
        Blocking sibling of :meth:`query_status` — returns the current
        status without spinning up an asyncio loop. Rejects calls made
        from inside a running asyncio loop.
        """
    def wait_till_complete_blocking(self, sleep_time: builtins.float = 0.25, max_attempts: builtins.int = 80) -> builtins.bool:
        r"""
        Blocking sibling of :meth:`wait_till_complete` — polls status
        until COMPLETE or NOT_FOUND without needing an asyncio loop.
        Rejects calls made from inside a running asyncio loop.

        Args:
            sleep_time: Time to sleep between status checks (seconds). Default: 0.25
            max_attempts: Maximum number of attempts before giving up. Default: 80 (20 seconds)

        Returns:
            True if task completed, False if max attempts reached
        """

class Key:
    @property
    def namespace(self) -> builtins.str: ...
    @property
    def set_name(self) -> builtins.str: ...
    @property
    def value(self) -> typing.Optional[typing.Any]: ...
    @property
    def digest(self) -> typing.Optional[builtins.str]: ...
    def __new__(cls, namespace: builtins.str, set: builtins.str, key: typing.Any) -> _aerospike_async_native.Key: ...
    @staticmethod
    def from_int_user_key(namespace: builtins.str, set: builtins.str, key: builtins.int) -> _aerospike_async_native.Key:
        r"""
        Fast-path Key constructor for the bench/per-op hot loop where
        the caller has an integer user_key. Skips the Python ``str(int)``
        conversion AND the ``PythonValue`` enum dispatch by going
        directly to ``Value::String(key.to_string())``. Matches the
        existing-cluster on-disk representation (string-typed user keys)
        — switch to ``from_int`` instead if you want int-typed user
        keys (a different on-server keyspace).

        Per-op cost drops from ~2 µs (PyO3 PythonValue dispatch +
        Python str()) to ~500 ns (positional PyO3 call + Rust string
        alloc). JSDK does the equivalent in one Java ``new Key(...)``
        call at ~50 ns; we close most of the gap.
        """
    @staticmethod
    def key_with_digest(namespace: builtins.str, set: builtins.str, digest: typing.Any) -> _aerospike_async_native.Key:
        r"""
        Create a Key from a namespace, set, and digest (20-byte hash).
        The digest can be provided as bytes or a hex-encoded string.
        """
    def __richcmp__(self, other: _aerospike_async_native.Key, op: int) -> builtins.bool: ...
    def __str__(self) -> builtins.str: ...
    def __repr__(self) -> builtins.str: ...
    def __copy__(self) -> _aerospike_async_native.Key: ...
    def __deepcopy__(self, _memo: dict) -> _aerospike_async_native.Key: ...

class List:
    @property
    def value(self) -> builtins.list[typing.Any]: ...
    @value.setter
    def value(self, value: typing.Sequence[typing.Any]) -> None: ...
    def __new__(cls, v: typing.Sequence[typing.Any]) -> _aerospike_async_native.List: ...
    def as_string(self) -> builtins.str:
        r"""
        Returns a string representation of the value.
        """
    def __str__(self) -> builtins.str: ...
    def __repr__(self) -> builtins.str: ...
    def __getitem__(self, idx: builtins.int) -> typing.Any: ...
    def __setitem__(self, idx: builtins.int, v: typing.Any) -> None: ...
    def __delitem__(self, idx: builtins.int) -> None: ...
    def __concat__(self, other: _aerospike_async_native.List) -> _aerospike_async_native.List: ...
    def __inplace_concat__(self, other: _aerospike_async_native.List) -> _aerospike_async_native.List: ...
    def __repeat__(self, times: builtins.int) -> _aerospike_async_native.List: ...
    def __inplace_repeat__(self, times: builtins.int) -> _aerospike_async_native.List: ...
    def __hash__(self) -> builtins.int: ...
    def __len__(self) -> builtins.int: ...
    def __richcmp__(self, other: typing.Any, op: int) -> builtins.bool: ...
    def __iter__(self) -> _aerospike_async_native.List: ...
    def __next__(self) -> typing.Optional[typing.Any]: ...

class ListOperation:
    r"""
    List bin operations. Create list operations used by the client's `operate()` method.
    """
    @staticmethod
    def get(bin_name: builtins.str, index: builtins.int) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List get operation (gets element at index).
        """
    @staticmethod
    def size(bin_name: builtins.str) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List size operation (gets list size).
        """
    @staticmethod
    def pop(bin_name: builtins.str, index: builtins.int) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List pop operation (pops and returns element at index).
        """
    @staticmethod
    def clear(bin_name: builtins.str) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List clear operation (clears the list).
        """
    @staticmethod
    def get_range(bin_name: builtins.str, index: builtins.int, count: builtins.int) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List get_range operation (gets range of elements).
        """
    @staticmethod
    def set(bin_name: builtins.str, index: builtins.int, value: typing.Any) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List set operation (sets element at index).
        """
    @staticmethod
    def set_with_policy(bin_name: builtins.str, policy: _aerospike_async_native.ListPolicy, index: builtins.int, value: typing.Any) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List set_with_policy operation (sets element at index with list policy).
        """
    @staticmethod
    def remove(bin_name: builtins.str, index: builtins.int) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List remove operation (removes element at index).
        """
    @staticmethod
    def remove_range(bin_name: builtins.str, index: builtins.int, count: builtins.int) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List remove_range operation (removes range of elements).
        """
    @staticmethod
    def get_range_from(bin_name: builtins.str, index: builtins.int) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List get_range_from operation (gets range from index to end).
        """
    @staticmethod
    def pop_range(bin_name: builtins.str, index: builtins.int, count: builtins.int) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List pop_range operation (pops range of elements).
        """
    @staticmethod
    def pop_range_from(bin_name: builtins.str, index: builtins.int) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List pop_range_from operation (pops range from index to end).
        """
    @staticmethod
    def remove_range_from(bin_name: builtins.str, index: builtins.int) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List remove_range_from operation (removes range from index to end).
        """
    @staticmethod
    def trim(bin_name: builtins.str, index: builtins.int, count: builtins.int) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List trim operation (trims list to range).
        """
    @staticmethod
    def append(bin_name: builtins.str, value: typing.Any, policy: _aerospike_async_native.ListPolicy) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List append operation (appends a value to the list, requires ListPolicy).
        """
    @staticmethod
    def append_items(bin_name: builtins.str, values: typing.Sequence[typing.Any], policy: _aerospike_async_native.ListPolicy) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List append_items operation (appends multiple values to the list, requires ListPolicy).
        """
    @staticmethod
    def insert(bin_name: builtins.str, index: builtins.int, value: typing.Any, policy: _aerospike_async_native.ListPolicy) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List insert operation (inserts a value at index, requires ListPolicy).
        """
    @staticmethod
    def insert_items(bin_name: builtins.str, index: builtins.int, values: typing.Sequence[typing.Any], policy: _aerospike_async_native.ListPolicy) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List insert_items operation (inserts multiple values at index, requires ListPolicy).
        """
    @staticmethod
    def increment(bin_name: builtins.str, index: builtins.int, value: builtins.int, policy: _aerospike_async_native.ListPolicy) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List increment operation (increments element at index by value, requires ListPolicy).
        """
    @staticmethod
    def increment_by_one(bin_name: builtins.str, index: builtins.int) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List increment_by_one operation (increments element at index by 1).
        """
    @staticmethod
    def increment_by_one_with_policy(bin_name: builtins.str, policy: _aerospike_async_native.ListPolicy, index: builtins.int) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List increment_by_one_with_policy operation (increments element at index by 1 with policy).
        """
    @staticmethod
    def sort(bin_name: builtins.str, flags: _aerospike_async_native.ListSortFlags) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List sort operation (sorts the list, requires ListSortFlags).
        """
    @staticmethod
    def set_order(bin_name: builtins.str, order: _aerospike_async_native.ListOrderType) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List set_order operation (sets list order ORDERED/UNORDERED).
        """
    @staticmethod
    def get_by_index(bin_name: builtins.str, index: builtins.int, return_type: _aerospike_async_native.ListReturnType) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List get_by_index operation (gets element by index with return type, requires ListReturnType).
        """
    @staticmethod
    def get_by_index_range(bin_name: builtins.str, index: builtins.int, count: typing.Optional[builtins.int], return_type: _aerospike_async_native.ListReturnType) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List get_by_index_range operation (gets elements by index range with return type, requires ListReturnType).
        If count is None, gets from index to end of list.
        """
    @staticmethod
    def get_by_rank(bin_name: builtins.str, rank: builtins.int, return_type: _aerospike_async_native.ListReturnType) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List get_by_rank operation (gets element by rank with return type, requires ListReturnType).
        """
    @staticmethod
    def get_by_rank_range(bin_name: builtins.str, rank: builtins.int, count: typing.Optional[builtins.int], return_type: _aerospike_async_native.ListReturnType) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List get_by_rank_range operation (gets elements by rank range with return type, requires ListReturnType).
        If count is None, gets from rank to end of list.
        """
    @staticmethod
    def get_by_value(bin_name: builtins.str, value: typing.Any, return_type: _aerospike_async_native.ListReturnType) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List get_by_value operation (gets elements by value with return type, requires ListReturnType).
        """
    @staticmethod
    def get_by_value_range(bin_name: builtins.str, begin: typing.Any, end: typing.Any, return_type: _aerospike_async_native.ListReturnType) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List get_by_value_range operation (gets elements by value range with return type, requires ListReturnType).
        """
    @staticmethod
    def get_by_value_list(bin_name: builtins.str, values: typing.Sequence[typing.Any], return_type: _aerospike_async_native.ListReturnType) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List get_by_value_list operation (gets elements by value list with return type, requires ListReturnType).
        """
    @staticmethod
    def get_by_value_relative_rank_range(bin_name: builtins.str, value: typing.Any, rank: builtins.int, count: typing.Optional[builtins.int], return_type: _aerospike_async_native.ListReturnType) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List get_by_value_relative_rank_range operation (gets elements by value relative rank range, requires ListReturnType).
        """
    @staticmethod
    def remove_by_index(bin_name: builtins.str, index: builtins.int, return_type: _aerospike_async_native.ListReturnType) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List remove_by_index operation (removes element by index with return type, requires ListReturnType).
        """
    @staticmethod
    def remove_by_index_range(bin_name: builtins.str, index: builtins.int, count: typing.Optional[builtins.int], return_type: _aerospike_async_native.ListReturnType) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List remove_by_index_range operation (removes elements by index range with return type, requires ListReturnType).
        If count is None, removes from index to end of list.
        """
    @staticmethod
    def remove_by_rank(bin_name: builtins.str, rank: builtins.int, return_type: _aerospike_async_native.ListReturnType) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List remove_by_rank operation (removes element by rank with return type, requires ListReturnType).
        """
    @staticmethod
    def remove_by_rank_range(bin_name: builtins.str, rank: builtins.int, count: typing.Optional[builtins.int], return_type: _aerospike_async_native.ListReturnType) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List remove_by_rank_range operation (removes elements by rank range with return type, requires ListReturnType).
        If count is None, removes from rank to end of list.
        """
    @staticmethod
    def remove_by_value(bin_name: builtins.str, value: typing.Any, return_type: _aerospike_async_native.ListReturnType) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List remove_by_value operation (removes elements by value with return type, requires ListReturnType).
        """
    @staticmethod
    def remove_by_value_list(bin_name: builtins.str, values: typing.Sequence[typing.Any], return_type: _aerospike_async_native.ListReturnType) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List remove_by_value_list operation (removes elements by value list with return type, requires ListReturnType).
        """
    @staticmethod
    def remove_by_value_range(bin_name: builtins.str, begin: typing.Any, end: typing.Any, return_type: _aerospike_async_native.ListReturnType) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List remove_by_value_range operation (removes elements by value range with return type, requires ListReturnType).
        """
    @staticmethod
    def remove_by_value_relative_rank_range(bin_name: builtins.str, value: typing.Any, rank: builtins.int, count: typing.Optional[builtins.int], return_type: _aerospike_async_native.ListReturnType) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List remove_by_value_relative_rank_range operation (removes elements by value relative rank range, requires ListReturnType).
        """
    @staticmethod
    def create(bin_name: builtins.str, order: _aerospike_async_native.ListOrderType, pad: builtins.bool, persist_index: builtins.bool) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List create operation (creates a list with order and persisted index).
        """
    @staticmethod
    def create_with_index(bin_name: builtins.str, order: _aerospike_async_native.ListOrderType) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List create_with_index operation (creates list with persisted index).
        """
    @staticmethod
    def set_order_with_index(bin_name: builtins.str, order: _aerospike_async_native.ListOrderType) -> _aerospike_async_native.ListOperation:
        r"""
        Create a List set_order_with_index operation (sets list order with persisted index).
        """
    def set_context(self, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.ListOperation:
        r"""
        Set the context for this operation. Used for nested CDT operations.
        """

class ListPolicy:
    @property
    def order(self) -> _aerospike_async_native.ListOrderType: ...
    @order.setter
    def order(self, value: _aerospike_async_native.ListOrderType) -> None: ...
    @property
    def write_flags(self) -> builtins.int: ...
    @write_flags.setter
    def write_flags(self, value: typing.Any) -> None: ...
    def __new__(cls, order: typing.Optional[_aerospike_async_native.ListOrderType] = None, write_flags: typing.Optional[typing.Any] = None) -> _aerospike_async_native.ListPolicy:
        r"""
        Create a new ListPolicy with the specified order and write flags.
        Default is unordered list with default write flags.
        write_flags may be ListWriteFlags or int (bitmask), e.g. ADD_UNIQUE | NO_FAIL.
        """

@typing.final
class ListReturnType:
    r"""
    List return type for CDT operations.

    Supports bitwise OR for combining with INVERTED flag:
        combined = ListReturnType.VALUE | ListReturnType.INVERTED
    """
    NONE: ListReturnType
    """Do not return a result."""
    INDEX: ListReturnType
    """Return index offset order."""
    REVERSE_INDEX: ListReturnType
    """Return reverse index offset order."""
    RANK: ListReturnType
    """Return value order."""
    REVERSE_RANK: ListReturnType
    """Return reverse value order."""
    COUNT: ListReturnType
    """Return count of items selected."""
    VALUE: ListReturnType
    """Return value for single key read and value list for range read."""
    EXISTS: ListReturnType
    """Return true if count > 0."""
    INVERTED: ListReturnType
    """Invert meaning of list command and return values. Can be OR'd with other return types."""

    def __or__(self, other: ListReturnType) -> ListReturnType:
        """Bitwise OR - allows combining return type with INVERTED flag."""
        ...
    def __and__(self, other: ListReturnType) -> ListReturnType:
        """Bitwise AND."""
        ...
    def __int__(self) -> builtins.int:
        """Convert to integer."""
        ...
    def __eq__(self, other: object) -> builtins.bool: ...
    def __ne__(self, other: object) -> builtins.bool: ...
    def __hash__(self) -> builtins.int: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class LoopVarPart:
    r"""
    Identifies which element of a loop variable to access in path expressions.

    Requires Aerospike Server version >= 8.1.1.
    """
    MAP_KEY: LoopVarPart
    """Map key part of the loop variable."""
    VALUE: LoopVarPart
    """Value part of the loop variable (list element or map value)."""
    INDEX: LoopVarPart
    """Index part of the loop variable (parent list index)."""

    def __eq__(self, other: object) -> builtins.bool: ...
    def __ne__(self, other: object) -> builtins.bool: ...
    def __hash__(self) -> builtins.int: ...
    def __repr__(self) -> builtins.str: ...
class Map:
    @property
    def value(self) -> builtins.dict[typing.Any, typing.Any]: ...
    @value.setter
    def value(self, value: typing.Mapping[typing.Any, typing.Any]) -> None: ...
    def __new__(cls, v: typing.Mapping[typing.Any, typing.Any]) -> _aerospike_async_native.Map: ...
    def as_string(self) -> builtins.str:
        r"""
        Returns a string representation of the value.
        """
    def __richcmp__(self, other: typing.Any, op: int) -> builtins.bool: ...
    def __str__(self) -> builtins.str: ...
    def __repr__(self) -> builtins.str: ...

class MapOperation:
    r"""
    Map bin operations. Create map operations used by the client's `operate()` method.
    """
    @staticmethod
    def size(bin_name: builtins.str) -> _aerospike_async_native.MapOperation:
        r"""
        Create a Map size operation (gets map size).
        """
    @staticmethod
    def clear(bin_name: builtins.str) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def put(bin_name: builtins.str, key: typing.Any, value: typing.Any, policy: _aerospike_async_native.MapPolicy) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def put_items(bin_name: builtins.str, items: typing.Sequence[tuple[typing.Any, typing.Any]], policy: _aerospike_async_native.MapPolicy) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def increment_value(bin_name: builtins.str, key: typing.Any, value: builtins.int, policy: _aerospike_async_native.MapPolicy) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def decrement_value(bin_name: builtins.str, key: typing.Any, value: builtins.int, policy: _aerospike_async_native.MapPolicy) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def get_by_key(bin_name: builtins.str, key: typing.Any, return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def remove_by_key(bin_name: builtins.str, key: typing.Any, return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def get_by_key_range(bin_name: builtins.str, begin: typing.Any, end: typing.Any, return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def remove_by_key_range(bin_name: builtins.str, begin: typing.Any, end: typing.Any, return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def get_by_index(bin_name: builtins.str, index: builtins.int, return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def remove_by_index(bin_name: builtins.str, index: builtins.int, return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def get_by_index_range(bin_name: builtins.str, index: builtins.int, count: builtins.int, return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def remove_by_index_range(bin_name: builtins.str, index: builtins.int, count: builtins.int, return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def get_by_index_range_from(bin_name: builtins.str, index: builtins.int, return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def remove_by_index_range_from(bin_name: builtins.str, index: builtins.int, return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def get_by_rank(bin_name: builtins.str, rank: builtins.int, return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def remove_by_rank(bin_name: builtins.str, rank: builtins.int, return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def get_by_rank_range(bin_name: builtins.str, rank: builtins.int, count: builtins.int, return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def remove_by_rank_range(bin_name: builtins.str, rank: builtins.int, count: builtins.int, return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def get_by_rank_range_from(bin_name: builtins.str, rank: builtins.int, return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def remove_by_rank_range_from(bin_name: builtins.str, rank: builtins.int, return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def get_by_value(bin_name: builtins.str, value: typing.Any, return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def remove_by_value(bin_name: builtins.str, value: typing.Any, return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def get_by_value_range(bin_name: builtins.str, begin: typing.Any, end: typing.Any, return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def remove_by_value_range(bin_name: builtins.str, begin: typing.Any, end: typing.Any, return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def get_by_key_list(bin_name: builtins.str, keys: typing.Sequence[typing.Any], return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def remove_by_key_list(bin_name: builtins.str, keys: typing.Sequence[typing.Any], return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def get_by_value_list(bin_name: builtins.str, values: typing.Sequence[typing.Any], return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def remove_by_value_list(bin_name: builtins.str, values: typing.Sequence[typing.Any], return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation: ...
    @staticmethod
    def set_map_policy(bin_name: builtins.str, policy: _aerospike_async_native.MapPolicy) -> _aerospike_async_native.MapOperation:
        r"""
        Create a Map set_map_policy operation (sets map policy).
        """
    @staticmethod
    def get_by_key_relative_index_range(bin_name: builtins.str, key: typing.Any, index: builtins.int, count: typing.Optional[builtins.int], return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation:
        r"""
        Create a Map get_by_key_relative_index_range operation (gets items by key relative index range, requires MapReturnType).
        """
    @staticmethod
    def get_by_value_relative_rank_range(bin_name: builtins.str, value: typing.Any, rank: builtins.int, count: typing.Optional[builtins.int], return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation:
        r"""
        Create a Map get_by_value_relative_rank_range operation (gets items by value relative rank range, requires MapReturnType).
        """
    @staticmethod
    def remove_by_key_relative_index_range(bin_name: builtins.str, key: typing.Any, index: builtins.int, count: typing.Optional[builtins.int], return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation:
        r"""
        Create a Map remove_by_key_relative_index_range operation (removes items by key relative index range, requires MapReturnType).
        """
    @staticmethod
    def remove_by_value_relative_rank_range(bin_name: builtins.str, value: typing.Any, rank: builtins.int, count: typing.Optional[builtins.int], return_type: _aerospike_async_native.MapReturnType) -> _aerospike_async_native.MapOperation:
        r"""
        Create a Map remove_by_value_relative_rank_range operation (removes items by value relative rank range, requires MapReturnType).
        """
    @staticmethod
    def create(bin_name: builtins.str, order: _aerospike_async_native.MapOrder) -> _aerospike_async_native.MapOperation:
        r"""
        Create a Map create operation (creates a map with order).
        """
    @staticmethod
    def create_with_index(bin_name: builtins.str, order: _aerospike_async_native.MapOrder) -> _aerospike_async_native.MapOperation:
        r"""
        Create a Map create_with_index operation (creates map with persisted index).
        """
    @staticmethod
    def set_policy(bin_name: builtins.str, policy: _aerospike_async_native.MapPolicy) -> _aerospike_async_native.MapOperation:
        r"""
        Create a Map set_policy operation (sets full map policy including order and persist_index).
        """
    def set_context(self, ctx: typing.Sequence[_aerospike_async_native.CTX]) -> _aerospike_async_native.MapOperation:
        r"""
        Set the context for this operation. Used for nested CDT operations.
        """

class MapPolicy:
    @property
    def order(self) -> _aerospike_async_native.MapOrder: ...
    @order.setter
    def order(self, value: _aerospike_async_native.MapOrder) -> None: ...
    @property
    def write_mode(self) -> _aerospike_async_native.MapWriteMode: ...
    @write_mode.setter
    def write_mode(self, value: _aerospike_async_native.MapWriteMode) -> None: ...
    @property
    def flags(self) -> _aerospike_async_native.MapWriteFlags:
        r"""
        Get the write flags as a MapWriteFlags variant.

        Note: this getter is lossy for combined bitmasks — if the underlying
        raw byte is a composite (e.g. ``CREATE_ONLY | NO_FAIL == 5``), the
        returned variant collapses to ``MapWriteFlags.DEFAULT``. Use
        :py:attr:`raw_flags` for the lossless ``int`` value.
        """
    @flags.setter
    def flags(self, value: typing.Any) -> None: ...
    @property
    def raw_flags(self) -> builtins.int:
        r"""
        Get the raw write-flags byte as ``int`` — lossless replacement for
        :py:attr:`flags` when combined bitmasks are in use.
        """
    @property
    def persist_index(self) -> builtins.bool: ...
    @persist_index.setter
    def persist_index(self, value: builtins.bool) -> None: ...
    def __new__(cls, order: typing.Optional[_aerospike_async_native.MapOrder] = None, write_mode: typing.Optional[_aerospike_async_native.MapWriteMode] = None, flags: typing.Optional[typing.Any] = None, persist_index: typing.Optional[builtins.bool] = None) -> _aerospike_async_native.MapPolicy:
        r"""
        Create a new MapPolicy with the specified order and write mode.
        Optionally pass flags and persist_index for server 4.3+ behavior.
        Flags may be MapWriteFlags or int (bitmask), e.g. CREATE_ONLY | PARTIAL | NO_FAIL.
        Default is unordered map with update write mode.
        """
    @classmethod
    def new_with_flags(cls, order: typing.Optional[_aerospike_async_native.MapOrder], flags: typing.Any) -> _aerospike_async_native.MapPolicy:
        r"""
        Create a new MapPolicy with order and write flags (server 4.3+).
        Flags may be MapWriteFlags or int (bitmask), e.g. CREATE_ONLY | PARTIAL | NO_FAIL.
        """
    @classmethod
    def new_with_flags_and_persisted_index(cls, order: typing.Optional[_aerospike_async_native.MapOrder], flags: typing.Any) -> _aerospike_async_native.MapPolicy:
        r"""
        Create a new MapPolicy with order, write flags, and persisted index (server 4.3+).
        Flags may be MapWriteFlags or int (bitmask), e.g. CREATE_ONLY | PARTIAL | NO_FAIL.
        """

@typing.final
class MapReturnType:
    r"""
    Map return type for CDT operations.

    Supports bitwise OR for combining with INVERTED flag:
        combined = MapReturnType.VALUE | MapReturnType.INVERTED
    """
    NONE: MapReturnType
    """Do not return a result."""
    INDEX: MapReturnType
    """Return key index order."""
    REVERSE_INDEX: MapReturnType
    """Return reverse key order."""
    RANK: MapReturnType
    """Return value order."""
    REVERSE_RANK: MapReturnType
    """Return reverse value order."""
    COUNT: MapReturnType
    """Return count of items selected."""
    KEY: MapReturnType
    """Return key for single key read and key list for range read."""
    VALUE: MapReturnType
    """Return value for single key read and value list for range read."""
    KEY_VALUE: MapReturnType
    """Return key/value items."""
    EXISTS: MapReturnType
    """Returns true if count > 0."""
    UNORDERED_MAP: MapReturnType
    """Returns an unordered map."""
    ORDERED_MAP: MapReturnType
    """Returns an ordered map."""
    INVERTED: MapReturnType
    """Invert meaning of map command and return values. Can be OR'd with other return types."""

    def __or__(self, other: MapReturnType) -> MapReturnType:
        """Bitwise OR - allows combining return type with INVERTED flag."""
        ...
    def __and__(self, other: MapReturnType) -> MapReturnType:
        """Bitwise AND."""
        ...
    def __int__(self) -> builtins.int:
        """Convert to integer."""
        ...
    def __eq__(self, other: object) -> builtins.bool: ...
    def __ne__(self, other: object) -> builtins.bool: ...
    def __hash__(self) -> builtins.int: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class ModifyFlags:
    r"""
    Flags controlling the behavior of a ``CdtOperation.modify_by_path`` operation.

    JSDK-shape namespace of plain ``int`` constants. Combine with bitwise OR — the
    result is a regular ``int`` suitable for ``CdtOperation.modify_by_path(..., flag=...)``.

    Requires Aerospike Server version >= 8.1.1.
    """
    DEFAULT: builtins.int
    """Default behavior — fails on type mismatches."""
    NO_FAIL: builtins.int
    """Ignore type errors instead of failing."""
class Node:
    @property
    def name(self) -> builtins.str:
        r"""
        Returns the node name.
        """
    @property
    def address(self) -> builtins.str:
        r"""
        Returns the node address.
        """
    @property
    def is_active(self) -> builtins.bool:
        r"""
        Returns true if the node is active.
        """
    @property
    def version(self) -> _aerospike_async_native.Version:
        r"""
        Returns the server version.
        """
    @property
    def host(self) -> tuple[builtins.str, builtins.int]:
        r"""
        Returns the node host as a tuple (hostname, port).
        """
    @property
    def failures(self) -> builtins.int:
        r"""
        Returns the count of connection failures for this node.
        """
    @property
    def partition_generation(self) -> builtins.int:
        r"""
        Returns the partition generation number.
        """
    @property
    def rebalance_generation(self) -> builtins.int:
        r"""
        Returns the rebalance generation number.
        """
    def aliases(self) -> typing.Awaitable[typing.List[typing.Tuple[str, int]]]:
        r"""
        Returns a list of host aliases for this node.
        """
    def info(self, command: builtins.str, *, policy: typing.Optional[_aerospike_async_native.AdminPolicy] = None) -> typing.Awaitable[typing.Dict[str, str]]:
        r"""
        Execute an info command on this node.
        """
    def __str__(self) -> builtins.str: ...
    def __repr__(self) -> builtins.str: ...

class Operation:
    r"""
    Python wrapper for Operation enum.
    """
    def __new__(cls) -> _aerospike_async_native.Operation: ...
    @staticmethod
    def get() -> _aerospike_async_native.Operation:
        r"""
        Create a Get operation (reads all bins).
        """
    @staticmethod
    def get_bin(bin_name: builtins.str) -> _aerospike_async_native.Operation:
        r"""
        Create a Get operation for a specific bin.
        """
    @staticmethod
    def put(bin_name: builtins.str, value: typing.Any) -> _aerospike_async_native.Operation:
        r"""
        Create a Put operation.
        """
    @staticmethod
    def get_header() -> _aerospike_async_native.Operation:
        r"""
        Create a GetHeader operation (metadata only, no bin data).
        """
    @staticmethod
    def delete() -> _aerospike_async_native.Operation:
        r"""
        Create a Delete operation.
        """
    @staticmethod
    def touch() -> _aerospike_async_native.Operation:
        r"""
        Create a Touch operation (updates TTL).
        """
    @staticmethod
    def add(bin_name: builtins.str, value: typing.Any) -> _aerospike_async_native.Operation:
        r"""
        Create an Add operation (increments/decrements integer bin value).
        """
    @staticmethod
    def append(bin_name: builtins.str, value: typing.Any) -> _aerospike_async_native.Operation:
        r"""
        Create an Append operation (appends to string bin value).
        """
    @staticmethod
    def prepend(bin_name: builtins.str, value: typing.Any) -> _aerospike_async_native.Operation:
        r"""
        Create a Prepend operation (prepends to string bin value).
        """

@typing.final
class PartitionFilter:
    @property
    def begin(self) -> builtins.int: ...
    @begin.setter
    def begin(self, value: builtins.int) -> None: ...
    @property
    def count(self) -> builtins.int: ...
    @count.setter
    def count(self, value: builtins.int) -> None: ...
    @property
    def digest(self) -> typing.Optional[builtins.str]: ...
    @digest.setter
    def digest(self, value: typing.Optional[builtins.str]) -> None: ...
    @property
    def partitions(self) -> typing.Any: ...
    @partitions.setter
    def partitions(self, value: typing.Optional[list]) -> None: ...
    def __new__(cls) -> _aerospike_async_native.PartitionFilter: ...
    def done(self) -> builtins.bool: ...
    @staticmethod
    def all() -> _aerospike_async_native.PartitionFilter: ...
    @staticmethod
    def by_id(id: builtins.int) -> _aerospike_async_native.PartitionFilter: ...
    @staticmethod
    def by_key(key: _aerospike_async_native.Key) -> _aerospike_async_native.PartitionFilter: ...
    @staticmethod
    def by_range(begin: builtins.int, count: builtins.int) -> _aerospike_async_native.PartitionFilter: ...

@typing.final
class PartitionStatus:
    @property
    def bval(self) -> typing.Optional[builtins.int]: ...
    @bval.setter
    def bval(self, value: typing.Optional[builtins.int]) -> None: ...
    @property
    def id(self) -> builtins.int: ...
    @property
    def retry(self) -> builtins.bool: ...
    @retry.setter
    def retry(self, value: builtins.bool) -> None: ...
    @property
    def digest(self) -> typing.Optional[builtins.str]: ...
    @digest.setter
    def digest(self, value: typing.Optional[builtins.str]) -> None: ...
    def __new__(cls, id: builtins.int) -> _aerospike_async_native.PartitionStatus:
        r"""
        Create a new PartitionStatus with the specified partition ID.

        The `retry` field defaults to `true`, and other fields can be set via setters.
        """
    def __getitem__(self, key: typing.Any) -> typing.Any:
        r"""
        Dictionary-style access for convenience (in addition to getters/setters).
        Supported keys: 'id', 'bval', 'retry', 'digest'
        Example: ps['id'], ps['bval'] = 123
        """
    def __setitem__(self, key: typing.Any, value: typing.Any) -> None:
        r"""
        Dictionary-style assignment for convenience (in addition to getters/setters).
        Supported keys: 'bval', 'retry', 'digest'
        Note: 'id' is read-only and cannot be set.
        """

class Privilege:
    @property
    def code(self) -> _aerospike_async_native.PrivilegeCode: ...
    @property
    def namespace(self) -> typing.Optional[builtins.str]: ...
    @property
    def set_name(self) -> typing.Optional[str]: ...
    def __new__(cls, code: _aerospike_async_native.PrivilegeCode, namespace: typing.Optional[builtins.str], set_name: typing.Optional[builtins.str]) -> _aerospike_async_native.Privilege: ...
    def as_string(self) -> builtins.str: ...
    def __str__(self) -> builtins.str: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class QueryPlan:
    r"""
    Result of a server query explain (phase 1).
    """
    @property
    def selection(self) -> _aerospike_async_native.QuerySelection: ...
    @property
    def namespace(self) -> builtins.str: ...
    @property
    def set_name(self) -> typing.Optional[str]: ...
    @property
    def ael(self) -> builtins.str: ...
    @property
    def index_name(self) -> typing.Optional[builtins.str]: ...
    @property
    def index_type(self) -> _aerospike_async_native.CollectionIndexType: ...
    @property
    def is_primary_index(self) -> builtins.bool: ...
    @property
    def is_secondary_index(self) -> builtins.bool: ...
    @property
    def is_filtered_out(self) -> builtins.bool: ...
    def filter_for_execute(self) -> typing.Optional[_aerospike_async_native.Filter]:
        r"""
        Build the execute ``Filter`` for a secondary-index plan.
        """

class QueryPolicy(_aerospike_async_native.BasePolicy):
    ...

@typing.final
class QueryWhereFlags:
    r"""
    Field ``44`` (WHERE) flag bits for server query explain (phase 1).

    Combine with bitwise OR and pass to :meth:`Client.query_explain` /
    :meth:`Client.query_explain_blocking` as ``explain_where_flags``.
    Omit the argument (or pass ``None``) for default explain
    (``QueryWhereFlags.EXPLAIN`` only).

    Requires Aerospike Server version >= 8.1.3.
    """
    ENC_VARINT: builtins.int
    """Bit 0 encoding selector — must remain clear for v1 wire."""
    EXPLAIN: builtins.int
    """Explain phase — server runs index planner only (always set on explain)."""
    REQUIRE_INDEX: builtins.int
    """Reject primary-index fallback on explain when combined with ``EXPLAIN``."""
    HARD_HINT: builtins.int
    """Require field ``21`` index name hint; fail if hint missing or not selected."""
class ReadPolicy(_aerospike_async_native.BasePolicy):
    ...

class Record:
    @property
    def bins(self) -> typing.Any: ...
    @property
    def generation(self) -> typing.Optional[builtins.int]: ...
    @property
    def ttl(self) -> typing.Optional[builtins.int]: ...
    @property
    def key(self) -> typing.Optional[_aerospike_async_native.Key]: ...
    def bin(self, name: builtins.str) -> typing.Optional[typing.Any]: ...
    def __str__(self) -> builtins.str: ...
    def __repr__(self) -> builtins.str: ...

class Recordset:
    r"""
    Virtual collection of records retrieved through queries and scans. During a query/scan,
    multiple threads will retrieve records from the server nodes and put these records on an
    internal queue managed by the recordset. The single user thread consumes these records from the
    queue.
    """
    @property
    def active(self) -> builtins.bool: ...
    def close(self) -> None: ...
    def partition_filter(self) -> typing.Awaitable[typing.Optional[PartitionFilter]]: ...
    def __aiter__(self) -> _aerospike_async_native.Recordset: ...
    def __anext__(self) -> typing.Any: ...
    def __iter__(self) -> _aerospike_async_native.Recordset: ...
    def __next__(self) -> _aerospike_async_native.Record: ...

@typing.final
class RegexFlag:
    r"""
    POSIX regex bit flags for ``FilterExpression.regex_compare``.

    JSDK-shape namespace of plain ``int`` constants. Bit values match the
    Aerospike server wire protocol (POSIX ``regex.h`` on glibc).

    Combine with bitwise OR, e.g. ``RegexFlag.ICASE | RegexFlag.NEWLINE``.
    """
    NONE: builtins.int
    """Use regex defaults."""
    EXTENDED: builtins.int
    """Use POSIX Extended Regular Expression syntax when interpreting regex."""
    ICASE: builtins.int
    """Do not differentiate case."""
    NOSUB: builtins.int
    """Do not report position of matches."""
    NEWLINE: builtins.int
    """Match-any-character operators don't match a newline."""
class RegisterTask:
    def query_status(self) -> typing.Awaitable[TaskStatus]: ...
    def wait_till_complete(self, sleep_time: builtins.float = 0.25, max_attempts: builtins.int = 80) -> typing.Awaitable[bool]:
        r"""
        Wait for the task to complete, polling status until COMPLETE or NOT_FOUND.

        Args:
            sleep_time: Time to sleep between status checks (seconds). Default: 0.25
            max_attempts: Maximum number of attempts before giving up. Default: 80 (20 seconds)

        Returns:
            True if task completed, False if max attempts reached
        """
    def query_status_blocking(self) -> _aerospike_async_native.TaskStatus:
        r"""
        Blocking sibling of :meth:`query_status` — returns the current
        status without spinning up an asyncio loop. Rejects calls made
        from inside a running asyncio loop.
        """
    def wait_till_complete_blocking(self, sleep_time: builtins.float = 0.25, max_attempts: builtins.int = 80) -> builtins.bool:
        r"""
        Blocking sibling of :meth:`wait_till_complete` — polls status
        until COMPLETE or NOT_FOUND without needing an asyncio loop.
        Rejects calls made from inside a running asyncio loop.

        Args:
            sleep_time: Time to sleep between status checks (seconds). Default: 0.25
            max_attempts: Maximum number of attempts before giving up. Default: 80 (20 seconds)

        Returns:
            True if task completed, False if max attempts reached
        """

@typing.final
class ResultCode:
    OK: _aerospike_async_native.ResultCode
    SERVER_ERROR: _aerospike_async_native.ResultCode
    KEY_NOT_FOUND_ERROR: _aerospike_async_native.ResultCode
    GENERATION_ERROR: _aerospike_async_native.ResultCode
    PARAMETER_ERROR: _aerospike_async_native.ResultCode
    KEY_EXISTS_ERROR: _aerospike_async_native.ResultCode
    BIN_EXISTS_ERROR: _aerospike_async_native.ResultCode
    CLUSTER_KEY_MISMATCH: _aerospike_async_native.ResultCode
    SERVER_MEM_ERROR: _aerospike_async_native.ResultCode
    TIMEOUT: _aerospike_async_native.ResultCode
    ALWAYS_FORBIDDEN: _aerospike_async_native.ResultCode
    PARTITION_UNAVAILABLE: _aerospike_async_native.ResultCode
    BIN_TYPE_ERROR: _aerospike_async_native.ResultCode
    RECORD_TOO_BIG: _aerospike_async_native.ResultCode
    KEY_BUSY: _aerospike_async_native.ResultCode
    SCAN_ABORT: _aerospike_async_native.ResultCode
    UNSUPPORTED_FEATURE: _aerospike_async_native.ResultCode
    BIN_NOT_FOUND: _aerospike_async_native.ResultCode
    DEVICE_OVERLOAD: _aerospike_async_native.ResultCode
    KEY_MISMATCH: _aerospike_async_native.ResultCode
    INVALID_NAMESPACE: _aerospike_async_native.ResultCode
    BIN_NAME_TOO_LONG: _aerospike_async_native.ResultCode
    FAIL_FORBIDDEN: _aerospike_async_native.ResultCode
    ELEMENT_NOT_FOUND: _aerospike_async_native.ResultCode
    ELEMENT_EXISTS: _aerospike_async_native.ResultCode
    ENTERPRISE_ONLY: _aerospike_async_native.ResultCode
    OP_NOT_APPLICABLE: _aerospike_async_native.ResultCode
    FILTERED_OUT: _aerospike_async_native.ResultCode
    LOST_CONFLICT: _aerospike_async_native.ResultCode
    XDR_KEY_BUSY: _aerospike_async_native.ResultCode
    QUERY_END: _aerospike_async_native.ResultCode
    SECURITY_NOT_SUPPORTED: _aerospike_async_native.ResultCode
    SECURITY_NOT_ENABLED: _aerospike_async_native.ResultCode
    NOT_AUTHENTICATED: _aerospike_async_native.ResultCode
    SECURITY_SCHEME_NOT_SUPPORTED: _aerospike_async_native.ResultCode
    INVALID_COMMAND: _aerospike_async_native.ResultCode
    INVALID_FIELD: _aerospike_async_native.ResultCode
    ILLEGAL_STATE: _aerospike_async_native.ResultCode
    INVALID_USER: _aerospike_async_native.ResultCode
    USER_ALREADY_EXISTS: _aerospike_async_native.ResultCode
    FORBIDDEN_PASSWORD: _aerospike_async_native.ResultCode
    UDF_BAD_RESPONSE: _aerospike_async_native.ResultCode
    INDEX_FOUND: _aerospike_async_native.ResultCode
    INDEX_NOT_FOUND: _aerospike_async_native.ResultCode
    INDEX_OOM: _aerospike_async_native.ResultCode
    INDEX_NOT_READABLE: _aerospike_async_native.ResultCode
    INDEX_GENERIC: _aerospike_async_native.ResultCode
    INDEX_NAME_MAX_LEN: _aerospike_async_native.ResultCode
    INDEX_MAX_COUNT: _aerospike_async_native.ResultCode
    QUERY_ABORTED: _aerospike_async_native.ResultCode
    QUERY_QUEUE_FULL: _aerospike_async_native.ResultCode
    QUERY_TIMEOUT: _aerospike_async_native.ResultCode
    QUERY_GENERIC: _aerospike_async_native.ResultCode
    MRT_BLOCKED: _aerospike_async_native.ResultCode
    MRT_VERSION_MISMATCH: _aerospike_async_native.ResultCode
    MRT_EXPIRED: _aerospike_async_native.ResultCode
    MRT_TOO_MANY_WRITES: _aerospike_async_native.ResultCode
    MRT_COMMITTED: _aerospike_async_native.ResultCode
    MRT_ABORTED: _aerospike_async_native.ResultCode
    MRT_ALREADY_LOCKED: _aerospike_async_native.ResultCode
    MRT_MONITOR_EXISTS: _aerospike_async_native.ResultCode
    def __richcmp__(self, other: _aerospike_async_native.ResultCode, op: int) -> builtins.bool: ...
    def __hash__(self) -> builtins.int: ...
    def __repr__(self) -> builtins.str: ...

class Role:
    @property
    def name(self) -> builtins.str:
        r"""
        Role name.
        """
    @property
    def privileges(self) -> builtins.list[_aerospike_async_native.Privilege]:
        r"""
        List of assigned privileges.
        """
    @property
    def allowlist(self) -> builtins.list[builtins.str]:
        r"""
        The list of allowable IP addresses.
        """
    @property
    def read_quota(self) -> builtins.int:
        r"""
        Maximum reads per second limit for the role.
        """
    @property
    def write_quota(self) -> builtins.int:
        r"""
        Maximum writes per second limit for the role.
        """

@typing.final
class SelectFlags:
    r"""
    Flags controlling the return value of a ``CdtOperation.select_by_path`` operation.

    JSDK-shape namespace of plain ``int`` constants. Combine with bitwise OR
    (``SelectFlags.VALUE | SelectFlags.NO_FAIL``) — the result is a regular ``int``
    suitable for ``CdtOperation.select_by_path(..., flag=...)``.

    Requires Aerospike Server version >= 8.1.1.
    """
    MATCHING_TREE: builtins.int
    """Return the full matching subtree (root to leaf), keeping only matched nodes."""
    VALUE: builtins.int
    """Return the values of the finally-selected nodes."""
    LIST_VALUE: builtins.int
    """Synonym for ``VALUE`` — clarifies list element expectations."""
    MAP_VALUE: builtins.int
    """Synonym for ``VALUE`` — clarifies map value expectations."""
    MAP_KEY: builtins.int
    """Return only the map keys of the finally-selected nodes."""
    MAP_KEY_VALUE: builtins.int
    """Return map key-value pairs of the finally-selected nodes."""
    NO_FAIL: builtins.int
    """Ignore type mismatches instead of failing."""
class ServerError(builtins.Exception):
    @property
    def result_code(self) -> _aerospike_async_native.ResultCode: ...
    @property
    def in_doubt(self) -> builtins.bool: ...
    def __new__(cls, _message: builtins.str, result_code: _aerospike_async_native.ResultCode, in_doubt: builtins.bool = False) -> _aerospike_async_native.ServerError: ...

class Statement:
    r"""
    Query statement parameters.
    """
    @property
    def filters(self) -> typing.Optional[builtins.list[_aerospike_async_native.Filter]]: ...
    @filters.setter
    def filters(self, value: typing.Optional[typing.Sequence[_aerospike_async_native.Filter]]) -> None: ...
    @property
    def set_name(self) -> typing.Optional[str]: ...
    @set_name.setter
    def set_name(self, value: typing.Optional[str]) -> None: ...
    def __new__(cls, namespace: builtins.str, set_name: typing.Optional[builtins.str] = None, bins: typing.Optional[typing.Sequence[builtins.str]] = None) -> _aerospike_async_native.Statement: ...
    def set_aggregate_function(self, package_name: builtins.str, function_name: builtins.str, function_args: typing.Optional[typing.Sequence[typing.Any]] = None) -> None:
        r"""
        Set Lua aggregation function parameters for query aggregation.

        Args:
            package_name: Name of the Lua package/module containing the aggregation function.
            function_name: Name of the Lua aggregation function.
            function_args: Optional list of arguments to pass to the function.
        """
    def set_operations(self, ops: typing.Sequence[typing.Any]) -> None:
        r"""
        Attach an ops projection. The server returns the result of these
        operations for each matching record instead of the bin set
        configured via ``bins``. Mutually exclusive with ``bins`` (the
        server uses ``operations`` if both are set).

        Foreground queries accept only read ops. Server versions before
        8.1.2 only accept the basic ``Read`` op here; 8.1.2+ also accepts
        CDT, expression, bit, and HLL reads.
        """

class TlsConfig:
    def __new__(cls, cafile: builtins.str) -> _aerospike_async_native.TlsConfig:
        r"""
        Create a new TlsConfig from CA certificate file.

        Args:
            cafile: Path to the CA certificate file (PEM format)

        Returns:
            TlsConfig instance configured with the CA certificate
        """
    @staticmethod
    def with_client_auth(cafile: builtins.str, certfile: builtins.str, keyfile: builtins.str) -> _aerospike_async_native.TlsConfig:
        r"""
        Create a TlsConfig with client authentication from certificate and key files.

        Args:
            cafile: Path to the CA certificate file (PEM format)
            certfile: Path to the client certificate file (PEM format)
            keyfile: Path to the client private key file (PEM format)

        Returns:
            TlsConfig instance configured with CA and client certificates
        """

@typing.final
class Txn:
    r"""
    Multi-Record Transaction handle.

    Pass a `Txn` instance to record operations to group them into a single
    atomic transaction. Call :meth:`Client.commit` or :meth:`Client.abort`
    to finalize the transaction.

    Example::

        txn = aerospike_async.Txn()
        # use txn in put/get policy.txn field
        status = await client.commit(txn)
    """
    @property
    def id(self) -> builtins.int:
        r"""
        Unique transaction ID assigned by the client.
        """
    @property
    def state(self) -> _aerospike_async_native.TxnState:
        r"""
        Current state of the transaction.
        """
    @state.setter
    def state(self, value: _aerospike_async_native.TxnState) -> None:
        r"""
        Force the transaction into a given state.

        Primarily useful for testing error paths that depend on a
        non-``OPEN`` transaction (commands must be issued against an
        ``OPEN`` transaction). Production code should let
        :meth:`Client.commit` / :meth:`Client.abort` drive state
        transitions.
        """
    @property
    def timeout(self) -> builtins.int:
        r"""
        Transaction timeout in seconds. Zero means use the server default.
        """
    @timeout.setter
    def timeout(self, value: builtins.int) -> None:
        r"""
        Set the transaction timeout in seconds.

        Must be set before the transaction is shared with a policy or
        operation. After the underlying ``Arc<Txn>`` has been cloned into
        a policy / builder the timeout is frozen and this raises
        :class:`ValueError`. In a transactional session, set the timeout
        immediately after entering the ``async with`` block (before the
        first ``execute()``).
        """
    @property
    def namespace(self) -> typing.Optional[builtins.str]:
        r"""
        Namespace in use by this transaction, if one has been set.
        """
    def __new__(cls) -> _aerospike_async_native.Txn:
        r"""
        Create a new multi-record transaction.
        """
    def __repr__(self) -> builtins.str: ...

class UdfRemoveTask:
    def query_status(self) -> typing.Awaitable[TaskStatus]: ...
    def wait_till_complete(self, sleep_time: builtins.float = 0.25, max_attempts: builtins.int = 80) -> typing.Awaitable[bool]:
        r"""
        Wait for the task to complete, polling status until COMPLETE or NOT_FOUND.

        Args:
            sleep_time: Time to sleep between status checks (seconds). Default: 0.25
            max_attempts: Maximum number of attempts before giving up. Default: 80 (20 seconds)

        Returns:
            True if task completed, False if max attempts reached
        """
    def query_status_blocking(self) -> _aerospike_async_native.TaskStatus:
        r"""
        Blocking sibling of :meth:`query_status` — returns the current
        status without spinning up an asyncio loop. Rejects calls made
        from inside a running asyncio loop.
        """
    def wait_till_complete_blocking(self, sleep_time: builtins.float = 0.25, max_attempts: builtins.int = 80) -> builtins.bool:
        r"""
        Blocking sibling of :meth:`wait_till_complete` — polls status
        until COMPLETE or NOT_FOUND without needing an asyncio loop.
        Rejects calls made from inside a running asyncio loop.

        Args:
            sleep_time: Time to sleep between status checks (seconds). Default: 0.25
            max_attempts: Maximum number of attempts before giving up. Default: 80 (20 seconds)

        Returns:
            True if task completed, False if max attempts reached
        """

class User:
    @property
    def user(self) -> builtins.str:
        r"""
        User name.
        """
    @property
    def roles(self) -> builtins.list[builtins.str]:
        r"""
        List of assigned roles.
        """
    @property
    def read_info(self) -> builtins.list[builtins.int]:
        r"""
        List of read statistics. List may be nil.
        Current statistics by offset are:

        0: read quota in records per second
        1: single record read command rate (TPS)
        2: read scan/query record per second rate (RPS)
        3: number of limitless read scans/queries

        Future server releases may add additional statistics.
        """
    @property
    def write_info(self) -> builtins.list[builtins.int]:
        r"""
        List of write statistics. List may be nil.
        Current statistics by offset are:

        0: write quota in records per second
        1: single record write command rate (TPS)
        2: write scan/query record per second rate (RPS)
        3: number of limitless write scans/queries

        Future server releases may add additional statistics.
        """
    @property
    def conns_in_user(self) -> builtins.int:
        r"""
        Number of currently open connections for the user
        """

class Version:
    @property
    def major(self) -> builtins.int: ...
    @property
    def minor(self) -> builtins.int: ...
    @property
    def patch(self) -> builtins.int: ...
    @property
    def build(self) -> builtins.int: ...
    def supports_partition_scan(self) -> builtins.bool:
        r"""
        Returns true if server supports partition scans (>= 4.9.0.3).
        """
    def supports_query_show(self) -> builtins.bool:
        r"""
        Returns true if server supports query-show command (>= 5.7.0.0).
        """
    def supports_batch_any(self) -> builtins.bool:
        r"""
        Returns true if server supports batch-index commands (>= 6.0.0.0).
        """
    def supports_partition_query(self) -> builtins.bool:
        r"""
        Returns true if server supports partition queries (>= 6.0.0.0).
        """
    def supports_app_id(self) -> builtins.bool:
        r"""
        Returns true if server supports app-id (>= 8.1.0.0).
        """
    def supports_cdt_path_expressions(self) -> builtins.bool:
        r"""
        Returns true if server supports CDT path expression operations
        (``select_by_path`` / ``modify_by_path``). Requires server >= 8.1.1.
        """
    def supports_enhanced_expression_api(self) -> builtins.bool:
        r"""
        Returns true if server supports the enhanced expression API:
        ``in_list``, ``map_keys``, ``map_values``, ``ctx_map_keys_in``,
        ``ctx_and_filter``. Requires server >= 8.1.2.
        """
    def supports_query_ops_projection_ext(self) -> builtins.bool:
        r"""
        Returns true if server supports extended read ops (CDT,
        expression, bit, HLL reads) in foreground query ops projection.
        Earlier servers only accept basic ``Read`` ops attached to a
        query statement. Requires server >= 8.1.2.
        """
    def supports_mrt(self) -> builtins.bool:
        r"""
        Returns true if server supports Multi-Record Transactions
        (MRT). Requires server >= 8.0.0.
        """
    def supports_query_selection(self) -> builtins.bool:
        r"""
        Returns true if server supports two-phase server query selection
        (field ``44`` WHERE explain → execute). Requires server >= 8.1.3.
        """
    def __str__(self) -> builtins.str: ...
    def __repr__(self) -> builtins.str: ...

class WritePolicy(_aerospike_async_native.BasePolicy):
    ...

@typing.final
class _LocalClient:
    r"""
    Sync-only Aerospike client that owns a per-thread `current_thread`
    Tokio runtime. Eliminates the cross-thread worker hop per op that the
    shared multi-thread runtime imposes on `Client_blocking()`'s sync path.

    Construct one per Python OS thread. The runtime + Client are tied
    to the thread that built them — accessing from a different thread
    raises (enforced by `#[pyclass(unsendable)]`). Use `threading.local()`
    in Python to manage thread-bound instances.

    Surface mirrors the `Client.*_blocking` methods PSDK's sync path
    calls. Each `*_local` method runs on the per-thread runtime via
    `block_on`, so completion returns on the SAME thread without any
    worker-pool hop.
    **Experimental — subject to removal.** Per-thread `current_thread`
    Tokio runtime client. Open caveat: cluster-tend multiplication at
    high thread counts.
    Default users should stick with [`Client`]. Underscore-prefixed name
    signals private / unstable status; opt in via PSDK's
    `SyncClient(current_thread_runtime=True)`.
    """
    def __new__(cls, policy: _aerospike_async_native.ClientPolicy, seeds: builtins.str) -> _aerospike_async_native._LocalClient: ...
    def get_blocking(self, key: _aerospike_async_native.Key, bins: typing.Optional[typing.Sequence[builtins.str]] = None, *, policy: typing.Optional[_aerospike_async_native.ReadPolicy] = None, policy_sc: typing.Optional[_aerospike_async_native.ReadPolicy] = None, filter_expression: typing.Optional[_aerospike_async_native.FilterExpression] = None, txn: typing.Optional[_aerospike_async_native.Txn] = None) -> _aerospike_async_native.Record: ...
    def put_blocking(self, key: _aerospike_async_native.Key, bins: typing.Mapping[builtins.str, typing.Any], *, policy: typing.Optional[_aerospike_async_native.WritePolicy] = None, policy_sc: typing.Optional[_aerospike_async_native.WritePolicy] = None, txn: typing.Optional[_aerospike_async_native.Txn] = None) -> None: ...
    def delete_blocking(self, key: _aerospike_async_native.Key, *, policy: typing.Optional[_aerospike_async_native.WritePolicy] = None) -> builtins.bool: ...
    def touch_blocking(self, key: _aerospike_async_native.Key, *, policy: typing.Optional[_aerospike_async_native.WritePolicy] = None) -> None: ...
    def exists_blocking(self, key: _aerospike_async_native.Key, *, policy: typing.Optional[_aerospike_async_native.ReadPolicy] = None) -> builtins.bool: ...
    def operate_blocking(self, key: _aerospike_async_native.Key, operations: typing.Sequence[typing.Any], *, policy: typing.Optional[_aerospike_async_native.WritePolicy] = None, policy_sc: typing.Optional[_aerospike_async_native.WritePolicy] = None, record_exists_action: typing.Optional[_aerospike_async_native.RecordExistsAction] = None, expiration: typing.Optional[_aerospike_async_native.Expiration] = None, generation: typing.Optional[builtins.int] = None, durable_delete: typing.Optional[builtins.bool] = None, filter_expression: typing.Optional[_aerospike_async_native.FilterExpression] = None, txn: typing.Optional[_aerospike_async_native.Txn] = None) -> _aerospike_async_native.Record: ...
    def is_strong_consistency(self, namespace: builtins.str) -> typing.Optional[builtins.bool]:
        r"""
        Returns whether ``namespace`` is configured for strong
        consistency on the cluster.  See :meth:`Client.is_strong_consistency`
        for semantics.
        """
    def info_blocking(self, command: builtins.str) -> builtins.dict[builtins.str, builtins.str]: ...

@typing.final
class AbortStatus(enum.Enum):
    OK = ...
    ALREADY_ABORTED = ...
    ROLL_BACK_ABANDONED = ...
    CLOSE_ABANDONED = ...

@typing.final
class AuthMode(enum.Enum):
    r"""
    Authentication mode for client connections.
    """
    NONE = ...
    r"""
    No authentication will be performed.
    """
    INTERNAL = ...
    r"""
    Uses internal authentication when user/password defined. Hashed password is stored
    on the server. Do not send clear password. This is the default.
    """
    EXTERNAL = ...
    r"""
    Uses external authentication (like LDAP) when user/password defined. Specific external
    authentication is configured on server. If TLSConfig is defined, sends clear password
    on node login via TLS. Will return an error if TLSConfig is not defined.
    """
    PKI = ...
    r"""
    Allows authentication and authorization based on a certificate. No user name or
    password needs to be configured. Requires TLS and a client certificate.
    Requires server version 5.7.0+
    """

@typing.final
class BitWriteFlags(enum.Enum):
    DEFAULT = ...
    CREATE_ONLY = ...
    UPDATE_ONLY = ...
    NO_FAIL = ...
    PARTIAL = ...

@typing.final
class BitwiseOverflowActions(enum.Enum):
    FAIL = ...
    SATURATE = ...
    WRAP = ...

@typing.final
class BitwiseResizeFlags(enum.Enum):
    DEFAULT = ...
    FROM_FRONT = ...
    GROW_ONLY = ...
    SHRINK_ONLY = ...

@typing.final
class CollectionIndexType(enum.Enum):
    r"""
    Secondary index collection type.
    """
    DEFAULT = ...
    LIST = ...
    MAP_KEYS = ...
    MAP_VALUES = ...

@typing.final
class CommitLevel(enum.Enum):
    COMMIT_ALL = ...
    COMMIT_MASTER = ...

@typing.final
class CommitStatus(enum.Enum):
    OK = ...
    ALREADY_COMMITTED = ...
    ROLL_FORWARD_ABANDONED = ...
    CLOSE_ABANDONED = ...

@typing.final
class ExpReadFlags(enum.Enum):
    r"""
    Expression read flags for expression operations.
    """
    DEFAULT = ...
    r"""
    Default.
    """
    EVAL_NO_FAIL = ...
    r"""
    Ignore failures caused by the expression resolving to unknown or a non-bin type.
    """

@typing.final
class ExpType(enum.Enum):
    r"""
    Expression Data Types for usage in some `FilterExpressions`
    """
    NIL = ...
    BOOL = ...
    INT = ...
    STRING = ...
    LIST = ...
    MAP = ...
    BLOB = ...
    FLOAT = ...
    GEO = ...
    HLL = ...

@typing.final
class ExpWriteFlags(enum.Enum):
    r"""
    Expression write flags for expression operations.
    """
    DEFAULT = ...
    r"""
    Default. Allow create or update.
    """
    CREATE_ONLY = ...
    r"""
    If bin does not exist, a new bin will be created.
    If bin exists, the operation will be denied.
    """
    UPDATE_ONLY = ...
    r"""
    If bin exists, the bin will be overwritten.
    If bin does not exist, the operation will be denied.
    """
    ALLOW_DELETE = ...
    r"""
    If expression results in nil value, then delete the bin.
    """
    POLICY_NO_FAIL = ...
    r"""
    Do not raise error if operation is denied.
    """
    EVAL_NO_FAIL = ...
    r"""
    Ignore failures caused by the expression resolving to unknown or a non-bin type.
    """

@typing.final
class GenerationPolicy(enum.Enum):
    NONE = ...
    EXPECT_GEN_EQUAL = ...
    EXPECT_GEN_GREATER = ...

@typing.final
class HLLWriteFlags(enum.Enum):
    r"""
    HLL write flags for HLL operations.
    """
    DEFAULT = ...
    r"""
    Default. Allow create or update.
    """
    CREATE_ONLY = ...
    r"""
    If the bin already exists, the operation will be denied.
    If the bin does not exist, a new bin will be created.
    """
    UPDATE_ONLY = ...
    r"""
    If the bin already exists, the bin will be overwritten.
    If the bin does not exist, the operation will be denied.
    """
    NO_FAIL = ...
    r"""
    Do not raise error if operation is denied.
    """
    ALLOW_FOLD = ...
    r"""
    Allow the resulting set to be the minimum of provided index bits.
    """

@typing.final
class IndexType(enum.Enum):
    r"""
    Underlying data type of secondary index.
    """
    NUMERIC = ...
    STRING = ...
    GEO2D_SPHERE = ...

@typing.final
class ListOrderType(enum.Enum):
    UNORDERED = ...
    r"""
    List is not ordered. This is the default.
    """
    ORDERED = ...
    r"""
    List is ordered.
    """

@typing.final
class ListSortFlags(enum.Enum):
    DEFAULT = ...
    r"""
    Default. Preserve duplicate values when sorting list.
    """
    DESCENDING = ...
    r"""
    Sort in descending order.
    """
    DROP_DUPLICATES = ...
    r"""
    Drop duplicate values when sorting list.
    """

@typing.final
class ListWriteFlags(enum.Enum):
    DEFAULT = ...
    r"""
    Default is the default behavior. It means: Allow duplicate values and insertions at any index.
    """
    ADD_UNIQUE = ...
    r"""
    AddUnique means: Only add unique values.
    """
    INSERT_BOUNDED = ...
    r"""
    InsertBounded means: Enforce list boundaries when inserting. Do not allow values to be inserted at index outside current list boundaries.
    """
    NO_FAIL = ...
    r"""
    NoFail means: do not raise error if a list item fails due to write flag constraints.
    """
    PARTIAL = ...
    r"""
    Partial means: allow other valid list items to be committed if a list item fails due to write flag constraints.
    """

@typing.final
class MapOrder(enum.Enum):
    UNORDERED = ...
    r"""
    Map is not ordered. This is the default.
    """
    KEY_ORDERED = ...
    r"""
    Order map by key.
    """
    KEY_VALUE_ORDERED = ...
    r"""
    Order map by key, then value.
    """

@typing.final
class MapWriteFlags(enum.Enum):
    DEFAULT = ...
    r"""
    Default. Allow create or update.
    """
    CREATE_ONLY = ...
    r"""
    If the key already exists, the item will be denied. If the key does not exist, a new item will be created.
    """
    UPDATE_ONLY = ...
    r"""
    If the key already exists, the item will be overwritten. If the key does not exist, the item will be denied.
    """
    NO_FAIL = ...
    r"""
    Do not raise error if a map item is denied due to write flag constraints.
    """
    PARTIAL = ...
    r"""
    Allow other valid map items to be committed if a map item is denied due to write flag constraints.
    """

@typing.final
class MapWriteMode(enum.Enum):
    UPDATE = ...
    r"""
    If the key already exists, the item will be overwritten.
    If the key does not exist, a new item will be created.
    """
    UPDATE_ONLY = ...
    r"""
    If the key already exists, the item will be overwritten.
    If the key does not exist, the write will fail.
    """
    CREATE_ONLY = ...
    r"""
    If the key already exists, the write will fail.
    If the key does not exist, a new item will be created.
    """

@typing.final
class PrivilegeCode(enum.Enum):
    r"""
    Privilege code for access control.
    """
    UserAdmin = ...
    r"""
    User can edit/remove other users.  Global scope only.
    """
    SysAdmin = ...
    r"""
    User can perform systems administration functions on a database that do not involve user
    administration.  Examples include server configuration.
    Global scope only.
    """
    DataAdmin = ...
    r"""
    User can perform UDF and SINDEX administration actions. Global scope only.
    """
    UDFAdmin = ...
    r"""
    User can perform user defined function(UDF) administration actions.
    Examples include create/drop UDF. Global scope only.
    Requires server version 6+
    """
    SIndexAdmin = ...
    r"""
    User can perform secondary index administration actions.
    Examples include create/drop index. Global scope only.
    Requires server version 6+
    """
    Read = ...
    r"""
    User can read data only.
    """
    ReadWrite = ...
    r"""
    User can read and write data.
    """
    ReadWriteUDF = ...
    r"""
    User can read and write data through user defined functions.
    """
    Write = ...
    r"""
    User can read and write data through user defined functions.
    """
    Truncate = ...
    r"""
    User can truncate data only.
    Requires server version 6+
    """
    MaskingAdmin = ...
    r"""
    User can perform data masking administration actions.
    Global scope only.
    """
    ReadMasked = ...
    r"""
    User can read masked data only.
    """
    WriteMasked = ...
    r"""
    User can write masked data only.
    """

@typing.final
class QueryDuration(enum.Enum):
    r"""
    Expected query duration. The server treats the query in different ways depending on the expected duration.
    This enum is ignored for aggregation queries, background queries and server versions < 6.0.
    """
    LONG = ...
    r"""
    Long specifies that the query is expected to return more than 100 records per node.
    """
    SHORT = ...
    r"""
    Short specifies that the query is expected to return less than 100 records per node.
    """
    LONG_RELAX_AP = ...
    r"""
    LongRelaxAP will treat query as a Long query, but relax read consistency for AP namespaces.
    """

@typing.final
class QuerySelection(enum.Enum):
    r"""
    Server query plan selection inferred from an explain response.
    """
    PRIMARY_INDEX = ...
    SECONDARY_INDEX = ...
    FILTERED_OUT = ...

@typing.final
class ReadModeAP(enum.Enum):
    r"""
    Read policy for AP (availability) namespaces.
    Indicates how duplicates should be consulted in a read operation.
    Only makes a difference during migrations and only applicable in AP mode.
    """
    ONE = ...
    r"""
    A single node should be involved in the read operation.
    """
    ALL = ...
    r"""
    All duplicates should be consulted in the read operation.
    """

@typing.final
class ReadModeSC(enum.Enum):
    r"""
    Read policy for SC (strong consistency) namespaces.
    Determines SC read consistency options.
    """
    SESSION = ...
    r"""
    Ensures this client will only see an increasing sequence of record versions.
    Client only reads from master. This is the default.
    """
    LINEARIZE = ...
    r"""
    Ensures all clients will only see an increasing sequence of record versions.
    Client only reads from master.
    """
    ALLOW_REPLICA = ...
    r"""
    The client may read from master or any full (non-migrating) replica.
    Increasing sequence of record versions is not guaranteed.
    """
    ALLOW_UNAVAILABLE = ...
    r"""
    The client may read from master or any full (non-migrating) replica or from
    unavailable partitions. Increasing sequence of record versions is not guaranteed.
    """

@typing.final
class RecordExistsAction(enum.Enum):
    r"""
    `RecordExistsAction` determines how to handle record writes based on record generation.
    """
    UPDATE = ...
    UPDATE_ONLY = ...
    REPLACE = ...
    REPLACE_ONLY = ...
    CREATE_ONLY = ...

@typing.final
class Replica(enum.Enum):
    r"""
    Priority of operations on database server.
    """
    MASTER = ...
    MASTER_PROLES = ...
    RANDOM = ...
    SEQUENCE = ...
    PREFER_RACK = ...

@typing.final
class SpecialValue(enum.Enum):
    r"""
    Markers for Aerospike CDT map/list range and value operations.
    These use dedicated wire particles, distinct from Python ``float('inf')`` or the ``"*"`` string.
    """
    NULL = ...
    r"""
    Null particle boundary (e.g. unbounded start of a key range).
    """
    INFINITY = ...
    r"""
    Positive infinity particle (e.g. unbounded end of a value range).
    """
    WILDCARD = ...
    r"""
    Wildcard particle for value matching.
    """

@typing.final
class TaskStatus(enum.Enum):
    NOT_FOUND = ...
    IN_PROGRESS = ...
    COMPLETE = ...

@typing.final
class TxnState(enum.Enum):
    OPEN = ...
    VERIFIED = ...
    COMMITTED = ...
    ABORTED = ...

@typing.final
class UDFLang(enum.Enum):
    r"""
    User-defined function (UDF) language.
    """
    LUA = ...
    r"""
    Lua embedded programming language.
    """

def geojson(geo_str: builtins.str) -> _aerospike_async_native.GeoJSON:
    r"""
    Convert a GeoJSON string or coordinate pair to a GeoJSON object.
    This matches the legacy client's aerospike.geojson() function.

    Accepts:
    - GeoJSON JSON string: '{"type": "Point", "coordinates": [-122.0, 37.0]}'
    - Coordinate pair string: "-122.0, 37.5" (longitude, latitude)
    """

def has_any_write_op(operations: typing.Sequence[typing.Any]) -> builtins.bool:
    r"""
    Inspect a Python list of `Operation` / `ExpOperation` / CDT / HLL ops and
    report whether any are *writes* (so the caller can choose between
    :class:`BatchReadOp` and :class:`BatchWriteOp` for the heterogeneous
    :meth:`Client.batch_stream` ops list).

    PSDK uses this from its streaming-batch builder to match the wire
    dispatch that the buffered :meth:`Client.batch_operate` does internally
    — a key-op-list with only reads (e.g. an AEL `select_from` expression)
    must land as `BatchOperation::read_ops`, not `BatchOperation::write`,
    or the server rejects the per-node group.
    """

def new_client(policy: _aerospike_async_native.ClientPolicy, seeds: builtins.str) -> typing.Awaitable[Client]: ...

def new_client_blocking(policy: _aerospike_async_native.ClientPolicy, seeds: builtins.str) -> _aerospike_async_native.Client:
    r"""
    Synchronously create and connect a Client.

    Unlike :func:`new_client`, this function does not require a running
    asyncio event loop. The returned :class:`Client` can only be used with
    the ``_blocking`` method variants — calling an async method on a client
    built this way raises ``RuntimeError`` because the completion bridge
    (which captures an asyncio loop at construction time) is not initialized.
    """

def null() -> typing.Any:
    r"""
    Return a null value for use in Aerospike operations.
    This is equivalent to Python None but represents an Aerospike null value.
    Matches the legacy client's aerospike.null() function.
    """

